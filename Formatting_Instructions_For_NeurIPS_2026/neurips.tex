\documentclass{article}

\PassOptionsToPackage{numbers,sort&compress}{natbib}
\usepackage{neurips_2026}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{microtype}
\usepackage{xcolor}

\newcommand{\R}{\mathbb{R}}
\newcommand{\softmin}{\operatorname*{softmin}}
\newtheorem{proposition}{Proposition}[section]

\title{CARAT: Class-Aware Robust Aggregation via Hidden Task Certificates for Poisoning-Resilient Federated Learning}

\author{%
  Anonymous Authors \\
  Anonymous Institution \\
  \texttt{anonymous@neurips.cc}
}

\begin{document}

\maketitle

\begin{abstract}
Federated learning servers must aggregate client updates without observing client data, making them vulnerable to both untargeted model poisoning and targeted backdoor attacks. Existing robust aggregators mainly rely on update geometry, coordinate-wise robust statistics, or alignment with a single trusted reference direction. These signals can be brittle when benign clients are heterogeneous and adaptive attackers remain close to the benign update manifold. We present \textbf{CARAT}, a class-aware robust aggregation rule based on \emph{hidden task certificates}. CARAT uses server-visible labeled reference data to instantiate multiple hidden, class-balanced micro-tasks and scores each client update by the loss decrease it induces on those tasks. It then combines robust norm clipping, common-radius evaluation, worst-task optimization through a soft minimum over certificate scores, coordinate-rank anomaly penalties, and capped-simplex weight optimization with temporal regularization. This design favors updates that are broadly useful across semantic sub-tasks while suppressing clients that remain structurally extreme in rank space. In a verified FedAvg/CIFAR100/FangAttack run, CARAT substantially improves over Mean and assigns zero weight to the attacker coalition in late rounds. Because the currently available runs disable test-time evaluation, this evidence should be read as an initial empirical validation rather than a complete benchmark; nevertheless, it supports the promise of task-aware aggregation and motivates a full clean-accuracy and ASR study.
\end{abstract}

\section{Introduction}

Federated learning (FL) enables many clients to optimize a shared model without centralizing their raw data \citep{mcmahan2017fedavg}. This decentralization is attractive for privacy, governance, and communication reasons, but it also makes the server fundamentally dependent on updates that it cannot directly verify. Poisoning is therefore a systems-level concern. Malicious clients can send updates that derail convergence, bias the learned decision boundary, or implant triggers that activate attacker-chosen behavior at inference time \citep{fang2020local,shejwalkar2021manipulating,bagdasaryan2020backdoor,wang2020tails,zhang2022neurotoxin}. In practice, FL security is often limited by the quality of the aggregation rule.

The difficulty is not only that attackers can be strong, but also that benign clients are not identical. Under non-i.i.d.\ data partitions, benign updates differ in direction, norm, and sparsity for legitimate reasons. This creates a persistent ambiguity for the server: a client may look unusual because it is malicious, or simply because its local data distribution is different. Many existing defenses struggle because they rely on signals that conflate these two cases. Pairwise distances can mistake benign heterogeneity for adversarial deviation; coordinate-wise trimming can ignore cross-coordinate structure; and single-reference trust scores can miss task-specific failures.

This observation motivates the central question behind CARAT: \emph{can the server judge a client update by testing whether it is useful across hidden semantic sub-tasks, rather than only by asking whether it resembles other updates?} We answer this question with \textbf{CARAT}, \emph{Class-Aware Robust Aggregation via Hidden Task Certificates}. CARAT assumes the server has access to labeled reference data from which it constructs a balanced probe pool. Rather than compressing this trusted information into one reference gradient, the server repeatedly samples hidden, class-balanced micro-tasks and evaluates each client update by the loss decrease it produces on those tasks. The resulting certificate matrix exposes a failure mode missed by geometry-only rules: an update may appear globally plausible while still degrading a subset of classes or semantic regions.

CARAT combines this task-aware signal with two additional mechanisms. First, it uses robust norm clipping and common-radius evaluation to prevent trivial scale advantages from dominating hidden-task comparisons. Second, it computes a coordinate-rank anomaly penalty that captures whether a client remains structurally extreme across many coordinates even after clipping. Aggregation weights are then optimized on a capped simplex using a worst-task softmin objective and temporal regularization toward the previous round's weights.

The resulting method is more expressive than conventional geometry-only aggregation while remaining compatible with a standard synchronous FL runtime. Conceptually, CARAT aims to separate \emph{benign diversity} from \emph{malicious inconsistency}: benign clients may differ, but useful updates should still help a range of hidden tasks; malicious clients may imitate benign norms or directions, but it is harder for them to remain simultaneously task-useful and rank-typical across rounds.

\paragraph{Contributions.}
Our contributions are as follows.
\begin{itemize}
\item We introduce hidden-task certificates as a task-aware server-side utility signal for robust FL aggregation, using multiple class-balanced micro-tasks rather than a single trusted direction.
\item We formulate CARAT as a capped-simplex weight optimization problem that combines worst-task utility, rank-space anomaly suppression, and temporal regularization.
\item We make explicit the implementation assumptions enforced in code, most importantly the server-side access to a labeled training dataset from which the probe pool is sampled.
\item We report the current empirical evidence available in the repository and distinguish it from the full clean-test and ASR benchmark that remains to be executed.
\end{itemize}

\section{Method}

\subsection{Problem setting}

We consider synchronous FL with $n$ clients and a global parameter vector $w_r \in \R^d$ at round $r$. Each client returns a message $u_i^r$. In the FLPoison runtime, CARAT first converts all client messages into a common vector-form delta representation $\Delta_i^r \in \R^d$ using the shared \texttt{prepare\_updates} utility: for FedAvg, $\Delta_i^r$ is the difference between the client model vector and the current global model; for FedSGD and FedOpt, $\Delta_i^r$ is the vector update already produced by the client. The server then aggregates $\{\Delta_i^r\}_{i=1}^n$ into a single global step. A fraction $\rho$ of clients may be malicious and may coordinate to craft adversarial updates.

\paragraph{Server knowledge.}
The current implementation assumes more than a tiny external probe set. At initialization, the server receives the full server-side \texttt{train\_dataset} object and samples a balanced probe pool from it. CARAT then constructs hidden probe tasks only from that sampled pool, but the method still assumes server access to labeled training data. In its present form, CARAT is therefore not a data-free robust aggregation rule.

\paragraph{Goal.}
The server aims to learn aggregation weights that preserve benign diversity, prevent a malicious coalition from dominating the global step, and remain robust under both attack and benign non-i.i.d.\ variability.

\subsection{CARAT overview}

CARAT is built around three design principles.
\begin{itemize}
\item \textbf{Task-aware validation.} If an update is genuinely useful, it should improve more than one hidden slice of the task rather than only a narrow subset of classes.
\item \textbf{Worst-case robustness.} A robust aggregator should care about the weakest hidden tasks, not only average probe improvement.
\item \textbf{Structure-aware anomaly detection.} Even after norm clipping, malicious updates may remain extreme in coordinate ordering; rank-space signals complement task-aware certificates.
\end{itemize}

\paragraph{Round-level procedure.}

At communication round $r$, CARAT performs the following operations.
\begin{enumerate}
\item Robustly clip the client updates to suppress obvious norm inflation.
\item Rescale the clipped updates to a common evaluation radius so that hidden-task comparison is not dominated by raw magnitude.
\item Sample hidden, class-balanced probe tasks from the trusted probe pool.
\item Evaluate each candidate update on every hidden task and form a task-by-client certificate matrix.
\item Compute coordinate-rank anomaly penalties from random coordinate subsets.
\item Optimize aggregation weights on a capped simplex using worst-task utility, anomaly suppression, and temporal regularization.
\item Aggregate the \emph{clipped} client updates with the optimized weights and apply the resulting server step.
\end{enumerate}

The implementation initializes the temporal prior with a uniform weight vector and reuses the previous round's solution as the starting point for the next round. CARAT does not use a single transformed representation for every subproblem: the normalized updates are used only for \emph{fair scoring} on hidden tasks, whereas the clipped updates are used for the final model step. The method thus separates \emph{evaluation geometry} from \emph{update geometry}.

\subsection{Hidden-task and anomaly scoring}

CARAT begins by sampling a balanced probe pool from the server-visible training dataset. At each round, it constructs $T$ hidden tasks by drawing a small number of examples per class from that pool. In the current implementation, the default configuration uses a probe pool of $512$ examples, $T=8$ hidden tasks, and $4$ examples per class within each task when available. If a class has fewer than the requested examples, sampling falls back to replacement; if balanced task construction fails entirely, the implementation falls back to a single random subset from the probe pool.

This class-balanced design is the source of CARAT's class-aware behavior. A malicious update may still appear reasonable in aggregate if it primarily harms minority or attacker-targeted classes; balanced hidden tasks make such failures more visible to the server.

\paragraph{Clipping and common-radius evaluation.}

Before task evaluation, CARAT robustly clips client updates. Let $\|\Delta_i\|_2$ denote the norm of client $i$'s update. The default implementation computes a clipping threshold $\tau_r$ with a MAD-based rule:
\begin{equation}
\tau_r = \operatorname{median}(\{\|\Delta_i\|_2\}) +
k \cdot 1.4826 \cdot \operatorname{MAD}(\{\|\Delta_i\|_2\}),
\label{eq:clip}
\end{equation}
with percentile-based alternatives also available in the configuration. The clipped update is
\begin{equation}
\bar{\Delta}_i = \min\left(1, \frac{\tau_r}{\|\Delta_i\|_2 + \varepsilon}\right)\Delta_i.
\end{equation}

CARAT then rescales each clipped update to a common evaluation radius $q_r$:
\begin{equation}
\tilde{\Delta}_i = q_r \frac{\bar{\Delta}_i}{\|\bar{\Delta}_i\|_2 + \varepsilon},
\label{eq:normalize}
\end{equation}
where $q_r$ is chosen as a quantile of the positive clipped norms. This step removes pure norm advantages when the server compares candidate updates on hidden tasks. Importantly, the normalized vectors are used only for evaluation; the final aggregation is computed on the clipped updates $\bar{\Delta}_i$.
Appendix Proposition~\ref{prop:directional-invariance} formalizes the key invariance induced by this normalization step.

\paragraph{Hidden-task certificates.}

For each hidden task $t$, let $\ell_t(w)$ denote the average cross-entropy loss of model $w$ on that task. CARAT defines a \emph{hidden-task certificate}
\begin{equation}
c_{i,t} = \ell_t(w_r) - \ell_t(w_r + \tilde{\Delta}_i).
\label{eq:cert}
\end{equation}
Here, ``certificate'' denotes an internal task-level utility score rather than a formal certified-robustness guarantee. Positive values indicate that client $i$ improves task $t$ relative to the current global model.

Operationally, CARAT loads the current global weights into a server-side probe model, evaluates the baseline hidden-task losses once, and then reevaluates the same tasks after applying each candidate update $w_r + \tilde{\Delta}_i^r$. Stacking these values yields a certificate matrix $C \in \R^{n \times T}$. Because different tasks may have different baselines and scales, CARAT robustly standardizes each column using the column median and MAD, preserving within-task ordering while making columns comparable.

\paragraph{Rank-space anomaly penalties.}

Hidden-task utility alone is insufficient, because an attacker may overfit to a subset of probe tasks while remaining structurally abnormal. CARAT therefore augments certificate scores with a coordinate-rank anomaly signal.

For a sampled coordinate set $S \subseteq \{1,\dots,d\}$, CARAT ranks clients coordinate-wise and measures the average normalized distance from the center rank:
\begin{equation}
r_i^{(S)} = \frac{1}{|S|}\sum_{j \in S}
\left|
\frac{\operatorname{rank}_{i,j}}{n-1} - \frac{1}{2}
\right|.
\label{eq:rank}
\end{equation}
The implementation repeats this procedure over several random coordinate subsets and takes the median across subsamples. A client receives a large anomaly score if its update remains persistently extreme across many coordinates. The raw anomaly scores are then robustly standardized, and only positive deviations are retained as penalties. The penalty is asymmetric: suspiciously extreme clients are downweighted, while unusually central clients are not specially rewarded.

\subsection{Weight optimization and aggregation}

CARAT chooses aggregation weights $\alpha \in \R^n$ on the capped simplex
\begin{equation}
\mathcal{A}(\hat{\rho}) =
\left\{
\alpha \in \R^n :
\alpha_i \ge 0,\;
\sum_{i=1}^n \alpha_i = 1,\;
\alpha_i \le \frac{1}{(1-\hat{\rho})n}
\right\},
\label{eq:capped-simplex}
\end{equation}
where $\hat{\rho}$ is an estimate of the attacker fraction. This cap prevents any single client from receiving too much mass and ensures that at least $(1-\hat{\rho})n$ clients must contribute nontrivially to the aggregate.
Appendix Proposition~\ref{prop:capped-simplex} states the corresponding feasibility and support guarantees.

Let $r \in \R^n$ denote the positive rank penalties and let $\alpha^{\mathrm{prev}}$ be the previous round's weights. CARAT solves
\begin{equation}
\max_{\alpha \in \mathcal{A}(\hat{\rho})}
\;\softmin_{\beta}(C^\top \alpha)
- \lambda_r r^\top \alpha
- \lambda_p \|\alpha - \alpha^{\mathrm{prev}}\|_2^2,
\label{eq:objective}
\end{equation}
where
\begin{equation}
\softmin_{\beta}(s_1,\dots,s_T)
= -\frac{1}{\beta}\log\left(\frac{1}{T}\sum_{t=1}^T e^{-\beta s_t}\right).
\label{eq:softmin}
\end{equation}
The first term rewards weight allocations that improve the weakest hidden tasks. The second penalizes structurally extreme clients. The third stabilizes weights across rounds and reduces unnecessary oscillation. Appendix Proposition~\ref{prop:softmin} records the basic bounds and gradient form of the softmin term used by the optimizer.

When the runtime knows the number of attackers, the code resolves $\hat{\rho}$ directly from \texttt{num\_adv}; otherwise it uses a configured default. Eq.~\eqref{eq:objective} also yields an interpretable selection rule. If a client performs poorly on even a few hidden tasks, the softmin term increases pressure to downweight that client. If a client remains extreme in rank space, the penalty term further decreases its mass. A client must therefore satisfy \emph{both} a semantic criterion and a structural criterion to retain influence. Appendix Proposition~\ref{prop:temporal-prior} further clarifies the role of the temporal prior among utility-equivalent feasible weights.

\paragraph{Final aggregation and complexity.}

After optimization, CARAT aggregates the clipped updates:
\begin{equation}
\Delta_{\mathrm{agg}} = \sum_{i=1}^n \alpha_i \bar{\Delta}_i.
\label{eq:aggregate}
\end{equation}
This yields the final server step.

Relative to standard aggregation, the main additional cost comes from evaluating $n$ candidate updates over $T$ hidden tasks and computing rank penalties on coordinate subsamples. Appendix Proposition~\ref{prop:complexity} summarizes the resulting per-round complexity. The implementation therefore exposes explicit controls for the probe pool size, the number of tasks, the number of sampled coordinates, the number of rank subsamples, and the number of projected-gradient steps, making the overhead tunable.

\section{Experiments}

\subsection{Experimental setup}

CARAT is implemented as a server aggregator in FLPoison and can be used with FedAvg, FedSGD, and FedOpt because the runtime converts all client messages into vector-form deltas before defense logic. The reproduced setup we can currently verify end-to-end uses the committed \texttt{FedAvg\_CIFAR100\_Resnet18.yaml} base config: 300 rounds, 20 clients, local epochs $4$, learning rate $0.05$, batch size $64$, and a ResNet-18 model on CIFAR100. The same base config sets \verb|evaluate=false|, so the available CARAT logs do \emph{not} contain clean-test accuracy, clean-test loss, ASR, or ASR loss.

The benchmark infrastructure is broader than the current verified CARAT run. The committed \texttt{cifar100.yaml} batch spec already sweeps i.i.d.\ and Dirichlet non-i.i.d.\ partitions with $\alpha \in \{0.1,0.5,1.0\}$, attacker fractions $\{0.2,0.3,0.4\}$, untargeted attacks \{NoAttack, MinMax, MinSum, ALIE, FangAttack\}, and baselines \{Mean, TriGuardFL, FLDetector, FLTrust, MultiKrum, NormClipping\}. CARAT is implemented and logs are available for an i.i.d.\ FangAttack run, but CARAT has not yet been inserted into the committed batch spec. We therefore separate \emph{benchmark scope supported by code} from \emph{empirical evidence already executed for CARAT}.

When evaluation is enabled, the coordinator records clean test accuracy and clean test loss every round, and for data-poisoning or hybrid attacks it additionally records ASR and ASR loss. The default CARAT hyperparameters in the defense preset are: probe pool size $512$, $8$ hidden tasks, $4$ probe samples per class, probe batch size $128$, probe-radius quantile $0.5$, MAD clipping with $k=2.5$, default attacker prior $\hat{\rho}=0.2$, certificate temperature $\beta=12.0$, rank weight $0.20$, prior weight $0.05$, $4096$ sampled coordinates, $5$ rank subsamples, $60$ optimization steps, and optimizer learning rate $0.5$.

\section{Results}

\subsection{Current empirical results}

Table~\ref{tab:preliminary} summarizes the strongest evidence currently verified end-to-end in the repository: single-seed FedAvg runs on CIFAR100 with an i.i.d.\ split, 20 clients, 20\% attackers (4 malicious client slots), ResNet-18, 300 rounds, and FangAttack. The available logs cover Mean, TriGuardFL, and CARAT under the same base training configuration. Because that configuration sets \verb|evaluate=false|, the table reports \emph{training-only} diagnostics and runtime rather than clean-test accuracy or ASR.

\begin{table}[t]
\centering
\small
\begin{tabular}{lcccc}
\toprule
Defense & Train acc. @299 & Train loss @299 & Mean round time (s) & Total runtime \\
\midrule
Mean & 0.5699 & 1.5187 & 1.66 & 8m25s \\
TriGuardFL & \textbf{0.6385} & 1.2932 & 7.37 & 37m07s \\
CARAT & 0.6286 & \textbf{1.2748} & 9.49 & 47m37s \\
\bottomrule
\end{tabular}
\caption{Current empirical evidence from repository logs for FedAvg on CIFAR100 under i.i.d.\ FangAttack. These are training-only diagnostics because the reproduced runs disable test-time evaluation.}
\label{tab:preliminary}
\end{table}

CARAT substantially improves over simple averaging in the verified run. Relative to Mean, it increases round-299 training accuracy by about $5.9$ points and reduces training loss by about $0.24$. At the same time, the current evidence does \emph{not} support a broader claim of uniform dominance across strong baselines: on this single training-only run, TriGuardFL finishes with slightly higher training accuracy, whereas CARAT attains the lowest training loss. We therefore interpret Table~\ref{tab:preliminary} as evidence that the method is functioning as intended, not as a complete comparative benchmark.

The most diagnostic CARAT-specific signal is the behavior of the learned weights. In this benchmark, the coordinator assigns the first \texttt{num\_adv} worker slots to attackers, so clients $0$--$3$ are malicious. Late in training, CARAT repeatedly logs weight vectors of the form
\[
\alpha = [0,0,0,0,0.0625,0.0625,\ldots,0.0625],
\]
which means the optimizer places zero mass on the entire malicious coalition while distributing the remaining mass uniformly over benign clients. At epoch 298, for example, the log records task scores $[0.2879, 0.2784, 0.2859, 0.2781, 0.2533, 0.3009, 0.2930, 0.2876]$, a softmin value of $0.2820$, evaluation radius $18.1484$, and clip norm $18.3227$. These quantities indicate that the implementation is doing more than hard filtering: it is identifying weak hidden tasks under a common scoring radius and then applying the optimized weights to the clipped updates used for aggregation.

These observations are encouraging, but they do not yet establish improved clean-test accuracy, reduced ASR, or robustness across non-i.i.d.\ regimes. A complete evaluation still requires reruns with \verb|evaluate=true| and inclusion of CARAT in the batch benchmark spec.

\section{Ablation Experiments}

The cleanest ablations are those already supported by the current configuration interface. The first ablation package should isolate:
\begin{itemize}
\item probe pool size,
\item number of hidden tasks,
\item per-class probe samples,
\item clipping rule, clipping threshold, and evaluation-radius quantile,
\item rank penalty weight $\lambda_r$,
\item prior weight $\lambda_p$,
\item softmin temperature $\beta$,
\item rank subsampling budget and optimizer steps.
\end{itemize}

These ablations are directly supported by the preset groups summarized in Table~\ref{tab:ablation-placeholder}. This design already supports a concrete methodological question: how much of CARAT's behavior comes from task construction, clipping and normalization, rank penalties, or temporal regularization?

Two additional structural ablations would strengthen the final paper but require small code patches rather than simple config changes: replacing the softmin objective with average utility, and bypassing hidden-task certificates entirely. The current implementation does not expose those switches as top-level parameters, so the paper should not imply that those ablations have already been run.

\begin{table}[t]
\centering
\small
\begin{tabular}{ll}
\toprule
Factor & Directly supported config keys \\
\midrule
Probe construction & \verb|num_probe_pool|, \verb|num_probe_tasks|, \verb|probe_samples_per_class| \\
Clipping / normalization & \verb|clip_method|, \verb|clip_k|, \verb|clip_percentile|, \verb|probe_radius_quantile| \\
Objective shaping & \verb|certificate_beta|, \verb|rank_weight|, \verb|prior_weight|, \verb|rho_hat| \\
Rank estimation & \verb|rank_num_coords|, \verb|rank_num_subsamples| \\
Optimizer budget & \verb|optimizer_steps|, \verb|optimizer_lr| \\
\bottomrule
\end{tabular}
\caption{Ablation axes already exposed by the current CARAT implementation. This is the safest starting point for a code-aligned ablation section.}
\label{tab:ablation-placeholder}
\end{table}

The final empirical narrative should first establish robustness with clean-test metrics, then evaluate backdoor settings using both ASR and clean utility, and finally use the supported ablations above to determine whether CARAT's gains come primarily from hidden-task validation, rank-space filtering, or their interaction.

\section{Limitations}

The most important limitation is the server-side data assumption. In the current code path, CARAT does not receive an externally curated probe pool; instead, it receives the server-visible training dataset and samples a balanced probe pool from it. This is a materially stronger assumption than fully data-free robust aggregation.

A second limitation is computational cost. Compared with lightweight rules such as Mean, CARAT adds repeated hidden-task forward passes, rank subsampling, and projected optimization. On the current FangAttack/CIFAR100 run, the mean round time is $9.49$s for CARAT versus $1.66$s for Mean and $7.37$s for TriGuardFL. Any final empirical case must therefore justify CARAT's extra server-side cost with corresponding clean-test or ASR gains, not only with training-time diagnostics.

A third limitation is empirical coverage. The strongest verified CARAT evidence is still a single-seed, training-only FangAttack run; the committed batch spec does not yet include CARAT, and the base configuration disables evaluation entirely. This means the current manuscript supports a methodology claim and an initial empirical result, but not yet a full benchmark claim.

A fourth limitation is adaptive security. An informed adversary could try to approximate the probe distribution, optimize against the clipping rule, or mimic benign rank patterns. CARAT is designed to raise the difficulty of such attacks by combining heterogeneous signals, but it does not eliminate the adaptive-attack problem. A stronger final version of the paper should therefore include at least one explicitly adaptive attack study.

\paragraph{Broader impact.}

Robust aggregation methods can improve the reliability of federated learning in settings where model failures carry real cost, including medical, financial, and safety-sensitive applications. At the same time, stronger defenses may shift attacker incentives rather than remove them. For this reason, the broader impact of CARAT lies not only in the method itself, but also in the benchmark and evaluation protocol through which it is stress-tested. Clear reporting of assumptions, ablations, and failure cases is therefore part of the contribution.

\section{Related Work}

Prior work on poisoning-resilient federated learning can be organized by the signal used to decide whether a client should influence the global model. At a high level, the literature relies on geometric centrality, coordinate-wise robustness, trusted reference data, historical behavior, or explicit backdoor diagnostics. This organization is useful for situating CARAT because the method changes the \emph{unit of validation} from client-to-client similarity to client-to-task utility.

\paragraph{Geometry-based Byzantine robustness.}
Geometry-based defenses seek updates that remain central under pairwise distances. Krum and its extensions are canonical examples \citep{blanchard2017krum,elmhamdi2018bulyan}. Their appeal lies in a clear robustness intuition: malicious clients should look isolated from the benign majority. The main limitation is that this intuition weakens under client heterogeneity. When benign updates are naturally dispersed, geometric centrality is no longer a reliable proxy for benignness.

\paragraph{Coordinate-wise robust statistics.}
Coordinate-wise median and trimmed mean replace or discard extreme coordinates independently \citep{yin2018median}. These methods are computationally simple and often surprisingly strong, but they intentionally ignore interactions across coordinates. As a result, they may miss attacks that preserve per-coordinate plausibility while corrupting higher-order structure in the update.

\paragraph{Heterogeneity-aware robust aggregation.}
The interaction between Byzantine robustness and non-i.i.d.\ data has become an important theme in its own right. In particular, prior work shows that many robust aggregators degrade sharply under heterogeneity and proposes bucketing as a simple way to adapt robust rules to non-i.i.d.\ settings \citep{karimireddy2022bucketing}. This line of work is directly relevant to CARAT because it starts from the same failure mode but addresses it from a different angle: instead of mixing client updates before aggregation, CARAT evaluates updates through hidden task utility.

\paragraph{Trust-based and reference-data defenses.}
Trust-based defenses inject additional server knowledge into the aggregation process. FLTrust uses a small trusted server dataset to compute a trust anchor and reweight clients by their alignment with that anchor \citep{cao2021fltrust}. Other methods use client history or representation-level diagnostics rather than explicit trusted data; examples include FoolsGold, FLAME, DeepSight, and FLDetector \citep{fung2020foolsgold,nguyen2022flame,rieger2022deepsight,zhang2022fldetector}. CARAT is most closely related to this family because it also relies on server-side labeled reference data. The methodological difference is that CARAT neither compresses trusted information into a single global direction nor uses the trusted pool merely for post-hoc inspection. Instead, it expands the trusted pool into multiple hidden micro-tasks, yielding a more semantically resolved notion of client utility.

\paragraph{Adaptive poisoning attacks.}
Modern poisoning attacks are explicitly optimized against robust aggregators. FangAttack, MinMax, MinSum, and ALIE are representative examples \citep{fang2020local,shejwalkar2021manipulating,baruch2019alie}. On the backdoor side, model replacement, edge-case backdoors, and durable attacks such as Neurotoxin show that malicious updates can remain highly effective even when they are tuned to preserve outward plausibility \citep{bagdasaryan2020backdoor,wang2020tails,zhang2022neurotoxin}. This trend matters because it reduces the value of one-dimensional defenses. If an attacker only needs to satisfy a single criterion, such as geometric centrality or bounded norm, adaptation becomes easier.

\paragraph{Backdoor-specific defenses.}
Several recent defenses explicitly target backdoor robustness in FL. CRFL combines clipping and smoothing to provide certified robustness guarantees under restricted perturbation regimes \citep{xie2021crfl}. FLAME and DeepSight instead use clustering and model-inspection signals to filter or sanitize suspicious updates \citep{nguyen2022flame,rieger2022deepsight}. CARAT is complementary to these methods: it uses trusted data, but deploys that data as a collection of hidden task probes rather than as a purely global trust direction or a post-hoc inspection signal. This design places CARAT between trust-based aggregation and task-aware validation.

\paragraph{Positioning of CARAT.}
The closest comparison points for CARAT are FLTrust, bucketing-based robust aggregation, and backdoor-oriented inspection defenses such as FLAME and DeepSight. Compared with FLTrust, CARAT uses trusted data to create \emph{many} hidden validation tasks rather than a single trusted direction. Compared with bucketing, CARAT does not attempt to reduce heterogeneity by averaging clients before robust aggregation; instead, it asks whether each client update remains useful across hidden semantic slices of the task. Compared with FLAME and DeepSight, CARAT intervenes directly at aggregation time rather than primarily through clustering or post-hoc model inspection. In short, CARAT is not mainly a new distance rule or inspection heuristic, but a task-aware aggregation mechanism for the regime in which benign heterogeneity and adaptive poisoning interact.

\section{Conclusion}

CARAT combines three ideas in a single aggregation rule: hidden class-balanced task certificates, rank-space anomaly suppression, and capped-simplex weight optimization. Together, these components aim to distinguish benign diversity from malicious inconsistency more effectively than geometry-only or single-reference defenses. The current implementation and logs already support a concrete methodological claim and an initial empirical claim: in a logged CIFAR100/FangAttack run, CARAT suppresses attacker weights and substantially improves over Mean, albeit with higher runtime and without yet surpassing every strong baseline on that single seed. The remaining step is to complete the empirical case with clean-test and ASR evaluation, multi-seed reruns, non-i.i.d.\ settings, and code-aligned ablations.

\bibliographystyle{unsrtnat}
\bibliography{ref}

\appendix

\section{Theoretical Analysis}

\begin{proposition}[Feasibility and support of the capped simplex]
\label{prop:capped-simplex}
Let
\[
\mathcal{A}(\hat{\rho})=
\left\{
\alpha \in \R^n:
\alpha_i \ge 0,\;
\sum_{i=1}^n \alpha_i = 1,\;
\alpha_i \le \frac{1}{(1-\hat{\rho})n}
\right\},
\]
with $\hat{\rho}<1$. Then $\mathcal{A}(\hat{\rho})$ is non-empty, and every $\alpha \in \mathcal{A}(\hat{\rho})$ satisfies
\[
|\operatorname{supp}(\alpha)| \ge (1-\hat{\rho})n.
\]
\end{proposition}

\begin{proof}[Proof sketch]
Let $u = 1/((1-\hat{\rho})n)$. Since $\hat{\rho}<1$, we have $u \ge 1/n$, so the uniform weight vector is feasible and the set is non-empty. For the support bound, if only $k$ coordinates of $\alpha$ are positive, then
\[
1=\sum_{i=1}^n \alpha_i \le k u = \frac{k}{(1-\hat{\rho})n},
\]
which implies $k \ge (1-\hat{\rho})n$. Therefore CARAT can suppress suspicious clients, but cannot collapse the aggregate onto an arbitrarily small subset of participants.
\end{proof}

\begin{proposition}[Softmin bounds and gradient form]
\label{prop:softmin}
Let $s \in \R^T$ and define
\[
\softmin_{\beta}(s)
= -\frac{1}{\beta}\log\left(\frac{1}{T}\sum_{t=1}^T e^{-\beta s_t}\right).
\]
Then
\[
\min_t s_t \le \softmin_{\beta}(s) \le \frac{1}{T}\sum_{t=1}^T s_t.
\]
Moreover, if $z(\alpha)=C^\top\alpha$ and
\[
\pi_t(\alpha)=\frac{\exp(-\beta z_t(\alpha))}{\sum_{\ell=1}^T \exp(-\beta z_\ell(\alpha))},
\]
then
\[
\nabla_\alpha \softmin_{\beta}(C^\top\alpha)=C\pi(\alpha).
\]
\end{proposition}

\begin{proof}[Proof sketch]
The lower and upper bounds follow from standard log-sum-exp inequalities: the exponential average is bounded below by the largest exponential term and above by the arithmetic mean. Monotonicity in each coordinate is immediate. Differentiating the softmin expression with respect to $z_t$ yields the Gibbs weights $\pi_t(\alpha)$, and the chain rule then gives $C\pi(\alpha)$. Hence the ascent direction is a convex combination of certificate columns, with more weight placed on tasks whose current aggregate utility is smaller.
\end{proof}

\begin{proposition}[Directional invariance of certificate scoring]
\label{prop:directional-invariance}
Suppose two nonzero clipped updates satisfy $\bar{\Delta}_i = a \bar{\Delta}_j$ for some scalar $a>0$. Then their normalized versions in Eq.~\eqref{eq:normalize} coincide, i.e.,
\[
\tilde{\Delta}_i=\tilde{\Delta}_j.
\]
Consequently, their hidden-task certificates are identical:
\[
c_{i,t}=c_{j,t}\qquad \text{for all } t \in \{1,\dots,T\}.
\]
\end{proposition}

\begin{proof}[Proof sketch]
Eq.~\eqref{eq:normalize} rescales every nonzero clipped update to the common radius $q_r$. Positive collinearity is therefore removed by normalization:
\[
q_r\frac{\bar{\Delta}_i}{\|\bar{\Delta}_i\|_2}
=
q_r\frac{a\bar{\Delta}_j}{\|a\bar{\Delta}_j\|_2}
=
q_r\frac{\bar{\Delta}_j}{\|\bar{\Delta}_j\|_2}.
\]
Substituting the resulting equality into Eq.~\eqref{eq:cert} gives identical task scores. Thus, after clipping, the certificate stage depends on direction rather than residual norm magnitude.
\end{proof}

\begin{proposition}[Temporal prior and projected-iterate invariants]
\label{prop:temporal-prior}
Define
\[
f(\alpha)=\softmin_{\beta}(C^\top\alpha)-\lambda_r r^\top\alpha-\lambda_p\|\alpha-\alpha^{\mathrm{prev}}\|_2^2.
\]
If $\alpha,\alpha' \in \mathcal{A}(\hat{\rho})$ satisfy $C^\top\alpha=C^\top\alpha'$ and $r^\top\alpha=r^\top\alpha'$, then
\[
f(\alpha)-f(\alpha')
=
-\lambda_p\!\left(\|\alpha-\alpha^{\mathrm{prev}}\|_2^2-\|\alpha'-\alpha^{\mathrm{prev}}\|_2^2\right).
\]
In addition, under projected gradient ascent
\[
\alpha^{(k+1)}=\Pi_{\mathcal{A}(\hat{\rho})}\!\left(\alpha^{(k)}+\eta \nabla f(\alpha^{(k)})\right),
\]
every iterate remains in $\mathcal{A}(\hat{\rho})$.
\end{proposition}

\begin{proof}[Proof sketch]
The first statement follows by direct substitution: if the certificate and rank terms match, the only remaining difference in objective value is the quadratic prior term. CARAT therefore prefers, among utility-equivalent feasible weights, the solution closer to $\alpha^{\mathrm{prev}}$. The second statement is immediate from the projection operator. Hence the optimizer preserves three round invariants throughout: $\alpha_i \ge 0$, $\sum_i \alpha_i =1$, and $\alpha_i \le 1/((1-\hat{\rho})n)$.
\end{proof}

\begin{proposition}[Per-round complexity]
\label{prop:complexity}
Let $n$ be the number of clients, $T$ the number of hidden tasks, $m$ the number of sampled coordinates for rank scoring, $S$ the number of rank subsamples, and $K$ the number of projected-gradient steps. Ignoring constant factors from batch size and model width, the extra defense-side work of CARAT scales as
\[
\text{probe-model forward passes} + O(Snm + KnT).
\]
\end{proposition}

\begin{proof}[Proof sketch]
CARAT performs one baseline probe evaluation plus $n$ candidate probe evaluations over the $T$ hidden tasks, then computes $S$ rank summaries over $m$ coordinates, and finally executes $K$ projected optimization steps over $n$ weights and $T$ task scores. In the current implementation, the probe-model forward passes dominate wall-clock time, while the rank and optimization stages are lower-order terms controlled by $m$, $S$, and $K$.
\end{proof}

\paragraph{Remark on assumptions.}
The theoretical object $D^{\mathrm{ref}}$ in the main text should be read as the probe pool \emph{after} subsampling. The code, however, obtains that pool by sampling from the full server-visible training dataset. Any final formal discussion should keep this distinction explicit, because it materially strengthens the server-side assumption relative to a fully data-free defense.

\section{Algorithm}

\paragraph{Inputs.}
Current global model $w_r$, client messages $\{u_i^r\}_{i=1}^n$, previous-round weights $\alpha^{\mathrm{prev}}$, server-visible training dataset, and CARAT hyperparameters.

\paragraph{Persistent state.}
CARAT maintains two pieces of state across rounds: a balanced probe pool sampled at initialization from the server-visible training dataset, and the previous-round weight vector $\alpha^{\mathrm{prev}}$, which serves as the temporal prior in the current round.

\paragraph{Initialization.}
At the first round, the implementation initializes $\alpha^{\mathrm{prev}}$ to the uniform vector. This makes the initial round unbiased across clients while still allowing later rounds to exploit temporal consistency once CARAT has observed several optimization trajectories.

\paragraph{Procedure.}
\begin{enumerate}
\item Convert client messages to vector-form deltas $\Delta_i^r$ using the common update-preparation utility.
\item Compute client norms, choose a clipping threshold with the configured MAD or percentile rule, and form clipped deltas $\bar{\Delta}_i^r$.
\item Choose an evaluation radius from the configured quantile of positive clipped norms and normalize clipped deltas to obtain $\tilde{\Delta}_i^r$.
\item Sample class-balanced hidden tasks from the fixed probe pool for round $r$; if strict balanced sampling fails, fall back to replacement and, if necessary, to a random subset rather than aborting the round.
\item Load the current global weights into the server-side probe model, evaluate baseline hidden-task losses, and compute certificate scores $c_{i,t} = \ell_t(w_r) - \ell_t(w_r + \tilde{\Delta}_i^r)$ for all clients and tasks.
\item Robustly standardize certificate columns, compute rank-space penalties from coordinate subsamples of $\bar{\Delta}_i^r$, and retain only positive standardized rank deviations.
\item Resolve $\hat{\rho}$, initialize weights from $\alpha^{\mathrm{prev}}$, and optimize
\[
\max_{\alpha \in \mathcal{A}(\hat{\rho})}
\softmin_{\beta}(C^\top\alpha)-\lambda_r r^\top\alpha-\lambda_p\|\alpha-\alpha^{\mathrm{prev}}\|_2^2
\]
by projected gradient ascent on the capped simplex.
\item Update $\alpha^{\mathrm{prev}}$ with the optimized solution.
\item Aggregate the clipped deltas $\bar{\Delta}_i^r$ with the optimized weights and return the resulting global step.
\end{enumerate}

\paragraph{Round invariants and failure handling.}
Two implementation choices are important for robustness. First, the optimizer never leaves the capped simplex, so every intermediate and final weight vector remains feasible. Second, probe-task construction is fail-soft rather than fail-stop: if strict class-balanced sampling is impossible, the code falls back to replacement or to a random subset, ensuring that the round can still proceed with a well-defined certificate matrix.

\paragraph{Outputs.}
Aggregated server update, updated temporal prior $\alpha^{\mathrm{prev}}$, and diagnostic logs including learned weights, task scores, softmin focus, rank penalties, evaluation radius, clipping threshold, and the identities of the hidden probe tasks used in the current round.

\section{Additional Experiments}

This section summarizes the current verified experimental scope together with the next empirical extensions already supported by the repository structure.

\paragraph{Currently verified run.}
The strongest directly verified CARAT run in the repository uses FedAvg, CIFAR100, ResNet-18, an i.i.d.\ partition, 20 clients, 4 attackers, FangAttack, 300 rounds, learning rate $0.05$, local epochs $4$, and \verb|evaluate=false|. The main text already reports the final training metrics and runtime of this run together with the corresponding Mean and TriGuardFL baselines.

\paragraph{Supported benchmark matrix.}
The committed \texttt{cifar100.yaml} spec already supports i.i.d.\ and non-i.i.d.\ splits with Dirichlet $\alpha \in \{0.1,0.5,1.0\}$, attacker fractions $\{0.2,0.3,0.4\}$, and untargeted attacks \{NoAttack, MinMax, MinSum, ALIE, FangAttack\}. The next revision should add CARAT into that matrix rather than relying on one-off runs.

\paragraph{Next additions.}
\begin{enumerate}
\item Add CARAT to the main batch experiment specs and rerun the CIFAR100 benchmark with \verb|evaluate=true| so that clean test accuracy and ASR are recorded every round.
\item Extend the verified runs from the current i.i.d.\ FangAttack setting to the non-i.i.d.\ Dirichlet settings already present in the committed benchmark spec.
\item Add at least one backdoor suite, such as BadNets or Neurotoxin, so that the paper reports both clean utility and ASR.
\item Run the config-supported ablations in Table~\ref{tab:ablation-placeholder}, prioritizing the number of hidden tasks, the rank and prior weights, and the clipping controls.
\item Report multi-seed means and variances, plus defense runtime overhead, before making any strong comparative claim against other robust aggregators.
\end{enumerate}

\end{document}
