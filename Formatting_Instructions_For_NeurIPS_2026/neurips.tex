\documentclass{article}

\PassOptionsToPackage{numbers,sort&compress}{natbib}
\usepackage{neurips_2026}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{bm}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{algorithm}
\usepackage{algorithmic}

\newcommand{\R}{\mathbb{R}}
\newcommand{\softmin}{\operatorname*{softmin}}
\newcommand{\makecell}[2][c]{\shortstack[#1]{#2}}
\newcommand{\multirow}[3]{#3}
\newcommand{\cellcolor}[1]{}
\newcommand{\subfloat}[2][]{%
  \begin{minipage}[t]{0.30\textwidth}
    \centering
    #2\\[0.4em]
    \footnotesize #1
  \end{minipage}%
}
\newcommand{\missingfigureplaceholder}{%
  \setlength{\fboxsep}{0pt}%
  \fbox{%
    \begin{minipage}[c][0.18\textheight][c]{0.28\textwidth}
      \centering
      \small Figure placeholder\\[0.4em]
      \footnotesize Figure unavailable
    \end{minipage}%
  }%
}
\newcommand{\safeincludegraphics}[2][]{%
  \IfFileExists{#2}{\includegraphics[#1]{#2}}{\missingfigureplaceholder}%
}
\newtheorem{proposition}{Proposition}[section]

\title{CARAT: Class-Aware Robust Aggregation for Model-Poisoning-Resilient Federated Learning}

% \author{%
%   Anonymous Authors \\
%   Anonymous Institution \\
%   \texttt{anonymous@neurips.cc}
% }

\begin{document}

\maketitle

\begin{abstract}
Federated learning servers aggregate client updates without seeing client data, which leaves training exposed to model-poisoning attacks. Most robust aggregators rely on update geometry, coordinate-wise robust statistics, or alignment with a single trusted reference direction. These signals weaken when benign clients are heterogeneous and adaptive attackers stay close to the benign update manifold. We propose \textbf{CARAT}, a class-aware robust aggregation rule built on \emph{hidden task certificates}. Given a small labeled server-side reference set, CARAT samples multiple hidden, class-balanced micro-tasks and scores each client update by the loss reduction it achieves across them. CARAT combines these task-conditioned scores with robust clipping, common-radius evaluation, coordinate-rank anomaly penalties, and capped-simplex weight optimization with temporal regularization. The result is an aggregator that favors updates that remain useful across diverse semantic slices while limiting the influence of structurally extreme clients. In a matched CIFAR-100/ResNet18 non-i.i.d.\ evaluation with $20\%$ malicious clients, CARAT ranks first against six baselines across ALIE, FangAttack, MinMax, and MinSum under $\alpha\in\{1,0.5\}$. We frame CARAT specifically as a defense against untargeted model poisoning under heterogeneous client data, with an evaluation protocol emphasizing class-rich benchmarks, matched trusted-data budgets, and class-sensitive metrics.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Federated learning (FL) lets many clients train a shared model without centralizing raw data \citep{mcmahan2017fedavg,bonawitz2017secureagg,kairouz2021federated}. This decentralization is attractive in privacy- and governance-sensitive settings, but it leaves the server in an asymmetric position: the global model must be updated from client messages that the server cannot directly verify. The aggregation rule therefore becomes a security-critical part of the FL system. Malicious clients can slow convergence or distort the decision boundary through model-poisoning updates that remain difficult to distinguish from benign client drift \citep{bhagoji2019adversarial,fang2020local,shejwalkar2021manipulating,baruch2019alie}.

The central difficulty is that benign heterogeneity and malicious behavior can look similar from the server's perspective. Under non-i.i.d.\ partitions, benign clients can differ substantially in update direction, norm, sparsity, and class emphasis \citep{zhao2018noniid,li2020fedprox,karimireddy2020scaffold,zhu2021noniidsurvey}. An unusual update may therefore reflect either a harmful client or a legitimately different local distribution. This ambiguity weakens defenses based on coarse notions of centrality. Pairwise-distance methods can mistake benign dispersion for adversarial deviation, coordinate-wise robust statistics discard cross-coordinate structure, and trusted-anchor approaches compress all auxiliary supervision into a single global reference direction \citep{blanchard2017krum,elmhamdi2018bulyan,yin2018median,xie2019zeno,chen2018draco,li2019rsa,pillutla2022rfa,cao2021fltrust}. Such signals are often too coarse to expose attacks whose effect is concentrated on a subset of classes or semantic regions.

CARAT addresses this problem by shifting validation from geometric similarity to task-conditioned utility. The server maintains a small labeled reference set and repeatedly instantiates hidden, class-balanced micro-tasks from it. Each client update is scored by the loss reduction it induces across these tasks, producing a certificate matrix that measures whether the update is broadly useful across semantic slices of the problem instead of merely close to the geometric center of the client population. CARAT then combines this signal with robust clipping, common-radius evaluation, coordinate-rank anomaly penalties, and capped-simplex weight optimization with temporal regularization.

This design targets FL deployments in which a modest server-side audit set is realistic, such as cross-silo or institutionally managed settings. It is not a fully data-free defense. Within that setting, CARAT aims to preserve benign diversity while reducing the influence of updates that are semantically harmful or structurally extreme. Our main contributions are as follows.

\begin{itemize}
\item We propose CARAT, a class-aware robust aggregation rule that validates client updates through hidden-task certificates constructed from server-side labeled reference data, replacing one-shot geometric trust signals with multi-task utility checks.
\item We formulate aggregation as a capped-simplex optimization problem that combines worst-task utility across hidden tasks, coordinate-rank anomaly suppression, and temporal regularization, yielding a robust weighting mechanism compatible with heterogeneous FL.
\item We provide a matched CIFAR-100/ResNet18 non-i.i.d.\ comparison in which CARAT ranks first against six baselines across four model-poisoning attacks under $\alpha\in\{1,0.5\}$, with seed counts reported explicitly for every CARAT result.
\end{itemize}

\section{Related Work}
\label{sec:related-work}

\paragraph{Model-poisoning attacks.}
Modern FL attacks are designed to remain effective under server-side defenses. Early work already showed that even constrained adversaries can poison the global model while remaining relatively stealthy \citep{bhagoji2019adversarial,fang2020local,shejwalkar2021manipulating}. In particular, attacks such as ALIE, MinMax, and MinSum craft malicious updates that evade simple geometric or statistical filters while still harming convergence or accuracy \citep{shejwalkar2021manipulating,baruch2019alie}. Surveys make the broader point explicit: once the server cannot inspect raw client data, model poisoning becomes a systems concern rather than an edge case \citep{kairouz2021federated,liu2022threats}.

\paragraph{Robust aggregation under heterogeneity.}
Classical Byzantine-robust aggregation rules infer benignness from centrality. Krum and related geometry-based methods favor updates that remain close to the client population under pairwise distances \citep{blanchard2017krum,elmhamdi2018bulyan}, while coordinate-wise median and trimmed mean suppress marginal extremes independently \citep{yin2018median}. Later variants extend this family through suspicion scoring, regularized aggregation, coding-based redundancy, and geometric-median aggregation \citep{xie2019zeno,li2019rsa,chen2018draco,pillutla2022rfa,yin2019saddle}. These methods are attractive because they require little server-side prior knowledge. Under heterogeneous data, however, they can still confuse legitimate benign dispersion with malicious deviation. This limitation has motivated methods that address heterogeneity more directly through optimization stabilization, resampling, bucketing, local-SGD analysis, gradient splitting, nearest-neighbor mixing, label-skew-aware aggregation, and entropy-based public-data guidance \citep{li2020fedprox,karimireddy2020scaffold,he2021resampling,karimireddy2022bucketing,data2021localsgd,liu2023gas,allouah2023fixing,bao2024boba,huang2024sdea,zhu2021noniidsurvey}.

\paragraph{Trusted-data and history-based defenses.}
A third line of work augments aggregation with trusted data, history, or auxiliary diagnostics. In collaborative learning, AUROR anticipated update screening for poisoning resistance \citep{shen2016auror}. In FL, FLTrust introduces a trusted server anchor, while FoolsGold and FLDetector rely on historical similarity diagnostics \citep{cao2021fltrust,fung2020foolsgold,zhang2022fldetector}. Recent work also highlights the cost of robustness itself, including clipping design and the tradeoff between Byzantine resilience and clean accuracy \citep{allouah2025arc,yang2025tension}. CARAT is closest to this broader defense-and-detection family, but it uses a different validation principle: instead of reducing trust to a single anchor direction, geometric similarity, or post-hoc clustering signal, it asks whether a client update remains useful across multiple hidden semantic sub-tasks.

\section{Preliminaries and Problem Statement}
\label{sec:problem}

\subsection{Federated Learning}

We consider synchronous FL with a server and a client population $\mathcal{N}=\{1,\dots,N\}$. Client $i$ holds a local dataset $\mathcal{D}_i$ and local objective $F_i(w)$. The standard global objective is
\begin{equation}
F(w)=\sum_{i=1}^N p_i F_i(w),
\qquad
p_i=\frac{|\mathcal{D}_i|}{\sum_{j=1}^N|\mathcal{D}_j|}.
\label{eq:fl-objective}
\end{equation}
At communication round $r$, the server broadcasts the current model $w_r$ to selected clients $\mathcal{S}_r$. Each selected client initializes $w_{i,r}^{0}=w_r$ and performs $K$ local stochastic-gradient steps,
\begin{equation}
w_{i,r}^{k+1}=w_{i,r}^{k}-\eta_r \nabla F_i(w_{i,r}^{k};\xi_{i,r}^{k}),
\qquad k=0,\dots,K-1.
\label{eq:local-update}
\end{equation}
The client then returns a model message that the server converts to an update vector
\begin{equation}
\Delta_i^r=w_{i,r}^{K}-w_r.
\label{eq:delta-def}
\end{equation}
A server-side aggregation rule maps the received updates to one global step,
\begin{equation}
w_{r+1}=w_r+\mathcal{A}\big(\{\Delta_i^r:i\in\mathcal{S}_r\}\big).
\label{eq:generic-aggregation}
\end{equation}
FedAvg is recovered when $\mathcal{A}$ is the sample-size-weighted mean. In heterogeneous FL, however, benign updates can differ sharply because client data are non-i.i.d.; this makes geometry-only filtering unreliable as a sole trust signal.

\subsection{Threat Model and Server-Side Reference Data}

At each round, a subset $\mathcal{M}_r\subseteq\mathcal{S}_r$ of selected clients may be malicious and coordinated. A malicious client may replace its benign update $\Delta_i^r$ with an arbitrary crafted update $\widetilde{\Delta}_i^r$ intended to reduce global test accuracy while remaining close enough to the benign update distribution to evade robust aggregation. The server observes only client messages and does not inspect client data.

CARAT assumes that the server maintains a small labeled reference set $\mathcal{D}^{\mathrm{ref}}$ covering the task label space. From this data it constructs a probe pool $\mathcal{P}$ and repeatedly samples hidden, class-balanced validation tasks. This assumption is stronger than fully data-free robust aggregation and should be treated as part of the threat model, not as an incidental implementation detail. The key difference from single-anchor trusted-data methods is that CARAT does not compress $\mathcal{D}^{\mathrm{ref}}$ into one trusted update direction; instead, it uses the reference data to generate multiple hidden semantic checks.

\subsection{Problem Statement}

The server must assign aggregation weights that suppress harmful updates while preserving legitimate client diversity. Formally, given a round-level update set $\{\Delta_i^r\}_{i\in\mathcal{S}_r}$ and reference data $\mathcal{D}^{\mathrm{ref}}$, the goal is to construct nonnegative weights $\alpha_i^r$ and an aggregate $\Delta_{\mathrm{agg}}^r$ such that malicious influence is bounded, benign non-i.i.d.\ specialization is not discarded merely for being geometrically atypical, and the resulting global model maintains clean utility under model-poisoning attacks. CARAT addresses this problem by validating updates through hidden-task utility rather than through centrality alone.

\section{CARAT}
\label{sec:method}

\subsection{Overview}

Figure~\ref{fig:carat-overview} organizes CARAT into four visual blocks: client updates, task-conditioned validation, robust weight optimization, and final aggregation. Algorithmically, these blocks correspond to three coordinated server-side stages. First, the server converts client messages into update vectors, clips their norms robustly, and rescales them to a common evaluation radius. Second, it evaluates each candidate update on multiple hidden class-balanced tasks and combines the resulting utility certificates with a coordinate-rank anomaly penalty. Third, it solves a capped-simplex optimization problem with a temporal prior to produce the aggregation weights and applies them to the clipped updates. The evaluation geometry is normalized across clients, while the final server step remains conservative because it aggregates clipped updates.

\begin{figure*}[!htbp]
\centering
\safeincludegraphics[width=\textwidth]{figures/Carat_design_nocaption.png}
\caption{Overview of CARAT. Non-i.i.d.\ client data can make diverse benign updates difficult to distinguish from poisoning updates. CARAT uses a small labeled server reference set to sample hidden class-balanced tasks, forms a certificate matrix from task-level loss reductions, learns capped-simplex aggregation weights with rank-anomaly and temporal-stability penalties, and updates the global model by a weighted average of clipped client updates.}
\label{fig:carat-overview}
\end{figure*}

\subsection{Stage 1: Common-Geometry Evaluation}

At round $r$, CARAT first converts client messages into vector-form deltas and computes their norms. Before any semantic evaluation, the server robustly clips client updates so that a small number of large-magnitude messages cannot dominate the comparison stage. Let $\|\Delta_i\|_2$ denote the norm of client $i$'s update. In our implementation, the clipping threshold $\tau_r$ is estimated by the MAD-based rule
\begin{equation}
\tau_r = \operatorname{median}(\{\|\Delta_i\|_2\}) +
k \cdot 1.4826 \cdot \operatorname{MAD}(\{\|\Delta_i\|_2\}),
\label{eq:clip}
\end{equation}
and the clipped update is
\begin{equation}
\bar{\Delta}_i = \min\left(1, \frac{\tau_r}{\|\Delta_i\|_2 + \varepsilon}\right)\Delta_i.
\end{equation}

CARAT then rescales each clipped update to a common evaluation radius $q_r$:
\begin{equation}
\tilde{\Delta}_i = q_r \frac{\bar{\Delta}_i}{\|\bar{\Delta}_i\|_2 + \varepsilon},
\label{eq:normalize}
\end{equation}
where $q_r$ is chosen as a quantile of the positive clipped norms. This normalization removes pure scale advantages during hidden-task comparison. The normalized vectors are used only for evaluation; the final model update is still computed from the clipped vectors. CARAT uses normalization to create a fair scoring geometry, not to define the actual optimization step taken by the server.

\subsection{Stage 2: Hidden-Task Certificates and Structural Penalties}

After preprocessing, CARAT samples $T$ hidden tasks from the probe pool $\mathcal{P}$, with each task constructed to be approximately class-balanced. This is what makes the method class-aware. A malicious update can remain globally plausible while degrading only a subset of classes or semantic regions. Repeated balanced hidden tasks make these localized failures harder to hide. For each hidden task $t$, let $\ell_t(w)$ denote the average cross-entropy loss of model $w$ on that task. CARAT defines a \emph{hidden-task certificate}
\begin{equation}
c_{i,t} = \ell_t(w_r) - \ell_t(w_r + \tilde{\Delta}_i).
\label{eq:cert}
\end{equation}
Here, ``certificate'' denotes an internal task-level utility score, not a formal certified-robustness guarantee. Positive values indicate that client $i$ improves task $t$ relative to the current global model.

Stacking these scores yields a certificate matrix $C \in \R^{n \times T}$. Because different tasks may have different intrinsic difficulty and baseline loss scale, CARAT robustly standardizes each column of $C$ before optimization. This preserves within-task ordering while making scores comparable across hidden slices. The resulting matrix gives the server a richer signal than client-to-client similarity alone: a client should be trusted because it remains broadly useful across hidden tasks, not merely because it lies near the geometric center of the client population.

Task utility alone is insufficient, because a malicious client may overfit the probe tasks or align with part of the semantic signal while remaining structurally extreme in parameter space. CARAT therefore augments hidden-task certificates with a coordinate-rank anomaly score. For a sampled coordinate set $S \subseteq \{1,\dots,d\}$, it ranks clients coordinate-wise and measures the average normalized distance from the center rank:
\begin{equation}
r_i^{(S)} = \frac{1}{|S|}\sum_{j \in S}
\left|
\frac{\operatorname{rank}_{i,j}}{n-1} - \frac{1}{2}
\right|.
\label{eq:rank}
\end{equation}
CARAT repeats this computation over several random coordinate subsets and aggregates the resulting values robustly. A client receives a large anomaly score if its update remains persistently extreme across many coordinates rather than on a few isolated entries. After robust standardization, only positive deviations are retained as penalties. The signal is intentionally asymmetric: suspicious extremeness is penalized, whereas unusual centrality is not explicitly rewarded. Together, the certificate matrix and rank penalty separate two notions that are often conflated in robust aggregation: whether an update is useful and whether it is structurally atypical.

\subsection{Stage 3: Capped-Simplex Weight Optimization}

CARAT maps these signals to aggregation weights by solving a constrained optimization problem on the capped simplex
\begin{equation}
\mathcal{A}(\hat{\rho}) =
\left\{
\alpha \in \R^n :
\alpha_i \ge 0,\;
\sum_{i=1}^n \alpha_i = 1,\;
\alpha_i \le \frac{1}{(1-\hat{\rho})n}
\right\},
\label{eq:capped-simplex}
\end{equation}
where $\hat{\rho}$ is an estimate of the attacker fraction. The cap prevents any single client from dominating the aggregate and encodes a conservative trust budget at the weight level: even when a client appears useful, it cannot absorb arbitrary influence within one round.

Let $r \in \R^n$ denote the positive rank penalties and let $\alpha^{\mathrm{prev}}$ be the previous round's weights. The diagram denotes the temporal penalty by $\mathrm{TV}$; in the implementation and analysis, CARAT uses the quadratic temporal-variation penalty $\mathrm{TV}_2(\alpha,\alpha^{\mathrm{prev}})=\|\alpha-\alpha^{\mathrm{prev}}\|_2^2$. CARAT solves
\begin{equation}
\max_{\alpha \in \mathcal{A}(\hat{\rho})}
\;\softmin_{\beta}(C^\top \alpha)
- \lambda_r r^\top \alpha
- \lambda_p \|\alpha - \alpha^{\mathrm{prev}}\|_2^2,
\label{eq:objective}
\end{equation}
where
\begin{equation}
\softmin_{\beta}(s_1,\dots,s_T)
= -\frac{1}{\beta}\log\left(\frac{1}{T}\sum_{t=1}^T e^{-\beta s_t}\right).
\label{eq:softmin}
\end{equation}
The first term is a smooth approximation to worst-task utility: large $\beta$ places more emphasis on the weakest hidden tasks, while finite $\beta$ preserves optimization stability. The second term penalizes structurally extreme clients, and the third term regularizes the solution toward the previous round to reduce unnecessary oscillation. Eq.~\eqref{eq:objective} therefore implements a simple decision rule: a client should retain influence only if it remains useful across hidden tasks and does not repeatedly appear as an outlier in rank space.

After optimization, CARAT aggregates the clipped updates:
\begin{equation}
\Delta_{\mathrm{agg}} = \sum_{i=1}^n \alpha_i \bar{\Delta}_i.
\label{eq:aggregate}
\end{equation}
This is the final server step. The aggregate inherits the robustness benefits of clipping while using weights learned from the richer evaluation geometry defined above. In practice, the main additional computation comes from evaluating $n$ candidate updates over $T$ hidden tasks and computing rank penalties on coordinate subsamples. Probe-model forward passes dominate, while the ranking and optimization stages remain tunable through explicit budget parameters.

A compact round-level algorithm summary is provided in Appendix~\ref{sec:appendix-algorithm}. Formal properties of the normalization step, capped-simplex feasibility, softmin behavior, temporal regularization, pairwise weight transfer, malicious-weight influence, and per-round complexity are deferred to Appendix~\ref{sec:appendix-theory}.

\section{Experiments}
\label{sec:experiments}

We evaluate CARAT on class-rich federated benchmarks to test whether hidden-task certificates improve robustness beyond geometry-only and single-anchor defenses while preserving clean utility under heterogeneous client data. The study is organized around three questions: how much clean-utility cost CARAT incurs without attack, how well it resists untargeted model poisoning as heterogeneity increases, and what robustness is obtained per unit of trusted-data budget and runtime overhead. The full benchmark matrix, appendix-only stress tests, and ablation scope are reported in Appendix~\ref{sec:appendix-experiments}.

\subsection{Experimental Setup}

\paragraph{Benchmarks.}
The primary benchmark is CIFAR-100 with ResNet18~\cite{he2016deep}. CIFAR-100 offers a class-rich label space in which hidden semantic slices are meaningful and class-selective failures are easier to expose than on low-cardinality datasets. We complement it with TinyImageNet, again using ResNet18, to test whether the method scales to a larger class vocabulary while keeping the backbone fixed. To anchor the method in a more application-oriented setting, we also include CHMNIST with the same backbone in the appendix, where class-selective degradation has a clearer domain interpretation. CIFAR-10 is retained only as an appendix sanity check rather than a primary source of evidence, because a ten-class benchmark is too coarse to serve as the main validation vehicle for a class-aware defense. Keeping the model family fixed across visual datasets helps attribute observed differences to aggregation rather than to backbone changes.

\paragraph{Threat models.}
For untargeted model poisoning, the main threat models are ALIE~\cite{baruch2019alie}, FangAttack~\cite{fang2020local}, MinMax~\cite{shejwalkar2021manipulating}, and MinSum~\cite{shejwalkar2021manipulating}. These attacks represent stealthy optimization-based adversaries that try to stay near the benign update manifold while degrading global learning, together with a geometry-aware attack that is especially informative for defenses based on Krum-style screening. We do not treat data poisoning or backdoor attacks as part of CARAT's claimed scope in this paper.

\paragraph{Baselines.}
The primary baselines are Mean, NormClipping, MultiKrum~\cite{blanchard2017krum}, FLTrust~\cite{cao2021fltrust}, FLDetector~\cite{zhang2022fldetector}, TriGuardFL, and CARAT. This set spans plain averaging, norm clipping, geometry-based filtering, trusted-data anchoring, history-based detection, and class-aware hidden-task validation. TrimmedMean~\cite{yin2018median} is retained for smaller i.i.d.\ verification runs, but it is not part of the main non-i.i.d.\ CIFAR-100 comparison in Table~\ref{tab:cifar100-main-acc}.

\paragraph{Implementation details.}
Across the primary comparison, we use $20$ clients with full participation, $200$ global rounds, $4$ local epochs, and learning rate $0.05$. To probe heterogeneity, we evaluate Dirichlet non-i.i.d.\ regimes with $\alpha \in \{1,0.5\}$ in the main table; the more severe $\alpha=0.1$ regime is treated as a stress-test setting and is not used for the central claim. The adversarial setting uses an attacker ratio of $20\%$, corresponding to $4$ malicious clients. Clean-reference experiments are reported separately from attacked experiments instead of being inserted as attack rows. Unless otherwise stated, baseline means and standard deviations are computed over $5$ seeds. CARAT seed counts are stated in the relevant table captions.

\begin{table*}[!htbp]
\centering
\caption{Experimental setup for evaluating CARAT.}
\label{tab:hyperparams}
\renewcommand{\arraystretch}{1.1}
\small
\begin{tabular}{p{0.28\textwidth}p{0.64\textwidth}}
\toprule
\textbf{Component} & \textbf{Design choice} \\
\midrule
Primary benchmark & CIFAR-100 with ResNet18 \\
Supplementary benchmarks & TinyImageNet with ResNet18; CHMNIST with ResNet18 (appendix); CIFAR-10 only as appendix sanity check \\
Clients and participation & $20$ total clients with full participation each round \\
Optimization schedule & $200$ communication rounds, $4$ local epochs, learning rate $0.05$ \\
Heterogeneity settings & Dirichlet non-i.i.d.\ with $\alpha \in \{1,0.5\}$ for the main comparison; $\alpha=0.1$ reserved for stress testing \\
Main adversarial fraction & $20\%$ malicious clients \\
Stress adversarial fraction & $40\%$ malicious clients (appendix) \\
Main untargeted attacks & ALIE, FangAttack, MinMax, MinSum \\
Untargeted baselines & Mean, NormClipping, MultiKrum, FLTrust, FLDetector, TriGuardFL, CARAT \\
Primary reported metrics & Final test accuracy, final test loss, average rank, runtime per round where available \\
Fairness controls & Matched labeled server-reference budget between CARAT and FLTrust; clean-reference runs reported separately from attacked runs \\
Repetitions & Five seeds for baseline rows; CARAT seed counts reported in the table captions \\
\bottomrule
\end{tabular}
\end{table*}

\paragraph{Metrics and fairness.}
Because CARAT is explicitly class-aware, the evaluation must report more than aggregate clean accuracy. For clean utility, we report test accuracy and test loss under no attack. For robustness, we additionally report macro-average accuracy and worst-class accuracy on class-rich benchmarks so that localized failures remain visible. Fairness requires explicit control of server-side supervision: CARAT and FLTrust are compared under a matched labeled-data budget, and the reference budget is parameterized per class rather than fixed as a dataset-independent total pool size.

\subsection{Main Evaluation Comparisons}

\paragraph{Clean utility.}
We first report no-attack performance to make the cost of robustness visible. These comparisons focus on test accuracy and test loss, with macro and worst-class accuracy added on class-rich benchmarks when aggregate accuracy alone could mask concentrated degradation.

\paragraph{Untargeted poisoning.}
We then compare CARAT against Mean, NormClipping, MultiKrum, FLTrust, FLDetector, and TriGuardFL under ALIE, FangAttack, MinMax, and MinSum on non-i.i.d.\ partitions. The main question is whether task-conditioned validation remains reliable when benign clients are dispersed and adaptive attackers stay close to the benign update manifold.

\subsection{Ablations, Stress Tests, and Overhead}

\paragraph{Ablations.}
We ablate the components that correspond directly to CARAT's mechanism: hidden-task count, reference budget per class, rank-penalty weight, temporal-prior weight, clipping controls, and common-radius evaluation. These studies isolate whether the observed robustness comes from class-balanced task validation, structural anomaly suppression, or simply from access to more trusted data.

\paragraph{Stress tests.}
Appendix-only stress tests raise the attacker fraction to $40\%$, examine the more severe $\alpha=0.1$ heterogeneity regime, and vary the client count or class vocabulary. Their role is to probe failure modes and scaling behavior rather than to replace the main-text comparisons.

\paragraph{Overhead analysis.}
Since Proposition~\ref{prop:complexity} identifies probe-model evaluation as the main extra cost, we report runtime per round, total defense overhead, and trusted-data budget sensitivity as first-class evaluation outcomes rather than as implementation footnotes.

\subsection{Main Results}

The main empirical comparison is a non-i.i.d.\ CIFAR-100/ResNet18 evaluation with $20$ clients, $20\%$ malicious clients, $200$ rounds, and four untargeted model-poisoning attacks. Table~\ref{tab:cifar100-main-acc} reports final test accuracy for this matched protocol. We average only successful 200-round runs under the same training and partition protocol. Baseline rows use five seeds in each cell. CARAT rows use the reported runs, with the exact seed counts stated in the caption.

\begin{table*}[!htbp]
\centering
\caption{Final test accuracy (\%) under four model-poisoning attacks on CIFAR-100/ResNet18 with $20\%$ malicious clients and non-i.i.d.\ client partitions. Higher is better. Baseline cells use five seeds. CARAT cells use $n=2$ for $\alpha=1$ ALIE, $n=1$ for $\alpha=0.5$ MinSum, and $n=3$ otherwise.}
\label{tab:cifar100-main-acc}
\renewcommand{\arraystretch}{1.05}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llccccc}
\toprule
\textbf{Dirichlet $\alpha$} & \textbf{Defense} & \textbf{ALIE} & \textbf{FangAttack} & \textbf{MinMax} & \textbf{MinSum} & \textbf{Avg. rank} \\
\midrule
\textbf{$1.0$} & Mean & $49.43 \pm 0.39$ & $48.86 \pm 0.70$ & $50.49 \pm 0.59$ & $50.50 \pm 0.49$ & $3.0$ \\
\textbf{$1.0$} & NormClipping & $25.27 \pm 0.70$ & $22.67 \pm 0.77$ & $24.86 \pm 0.65$ & $24.74 \pm 0.63$ & $7.0$ \\
\textbf{$1.0$} & MultiKrum & $39.59 \pm 0.74$ & $37.18 \pm 0.83$ & $48.69 \pm 0.84$ & $47.71 \pm 1.12$ & $4.8$ \\
\textbf{$1.0$} & FLTrust & $33.26 \pm 0.63$ & $33.81 \pm 0.99$ & $33.61 \pm 0.86$ & $33.85 \pm 0.78$ & $5.8$ \\
\textbf{$1.0$} & FLDetector & $47.96 \pm 0.58$ & $28.54 \pm 4.42$ & $50.58 \pm 0.76$ & $50.47 \pm 0.27$ & $4.0$ \\
\textbf{$1.0$} & TriGuardFL & $49.44 \pm 0.56$ & $51.01 \pm 0.93$ & $50.36 \pm 0.59$ & $50.65 \pm 0.52$ & $2.5$ \\
\textbf{$1.0$} & \textbf{CARAT} & \textbf{52.92$\pm$1.71} & \textbf{53.96$\pm$0.86} & \textbf{54.20$\pm$1.39} & \textbf{54.23$\pm$1.73} & \textbf{1.0} \\
\midrule
\textbf{$0.5$} & Mean & $47.62 \pm 0.73$ & $47.53 \pm 1.01$ & $48.88 \pm 0.32$ & $48.88 \pm 0.52$ & $3.2$ \\
\textbf{$0.5$} & NormClipping & $23.73 \pm 0.53$ & $20.64 \pm 1.41$ & $23.28 \pm 0.40$ & $22.92 \pm 0.50$ & $7.0$ \\
\textbf{$0.5$} & MultiKrum & $37.00 \pm 0.81$ & $33.05 \pm 1.20$ & $46.59 \pm 1.09$ & $46.45 \pm 0.80$ & $4.8$ \\
\textbf{$0.5$} & FLTrust & $31.72 \pm 0.51$ & $32.20 \pm 0.47$ & $32.21 \pm 0.68$ & $32.08 \pm 0.46$ & $5.8$ \\
\textbf{$0.5$} & FLDetector & $45.30 \pm 0.75$ & $27.77 \pm 1.25$ & $48.85 \pm 0.53$ & $49.01 \pm 0.69$ & $4.2$ \\
\textbf{$0.5$} & TriGuardFL & $47.81 \pm 0.63$ & $49.67 \pm 0.60$ & $49.12 \pm 0.68$ & $49.16 \pm 0.81$ & $2.0$ \\
\textbf{$0.5$} & \textbf{CARAT} & \textbf{51.89$\pm$0.56} & \textbf{52.58$\pm$1.30} & \textbf{53.76$\pm$0.36} & \textbf{54.81} & \textbf{1.0} \\
\bottomrule
\end{tabular}
}
\end{table*}

CARAT obtains the best average rank in both heterogeneity regimes and is the top method in every attack column reported in Table~\ref{tab:cifar100-main-acc}. Under $\alpha=1$, CARAT improves over the strongest non-CARAT result by $2.95$--$3.62$ percentage points across the four attacks. Under the more heterogeneous $\alpha=0.5$ setting, CARAT improves by $2.91$--$4.64$ points in the three multi-seed CARAT cells. The $\alpha=0.5$ MinSum cell is reported from one successful run, so we use it for ranking transparency but avoid drawing variance-sensitive conclusions from it. FangAttack is especially informative: FLDetector drops to $28.54\%$ and $27.77\%$ under $\alpha=1$ and $\alpha=0.5$, respectively, while CARAT remains at $53.96\%$ and $52.58\%$. This supports the main design claim that hidden-task validation can preserve useful heterogeneous updates while rejecting semantically harmful ones that evade or destabilize geometry-based screening.

Figure~\ref{fig:cifar100-fangattack-curves} gives the corresponding round-wise view for FangAttack, the attack that most sharply separates the defenses in Table~\ref{tab:cifar100-main-acc}. CARAT converges more slowly than the clean Mean reference in the early phase, but it continues improving after the geometry- and history-based defenses flatten or degrade. By the end of training, CARAT is the only attacked defense that reaches or exceeds the clean Mean reference in both heterogeneity regimes.

\begin{figure*}[!htbp]
\centering
\safeincludegraphics[width=0.98\textwidth]{figures/cifar100_fangattack_curves.pdf}
\caption{Evolution of test accuracy on CIFAR-100/ResNet18 under FangAttack with $20\%$ malicious clients and non-i.i.d.\ data. Curves show the mean over reported seeds; the CARAT band shows one standard deviation. The dashed black curve is the no-attack Mean reference under the same client partition protocol.}
\label{fig:cifar100-fangattack-curves}
\end{figure*}

The corresponding final-loss summaries in Table~\ref{tab:cifar100-main-loss} follow the same ranking pattern: CARAT has average rank $1.0$ under both $\alpha=1$ and $\alpha=0.5$. The clean Mean references are $52.57\pm0.41\%$ for $\alpha=1$ and $51.32\pm0.85\%$ for $\alpha=0.5$, which places CARAT close to the clean-utility regime even under attack. Additional verification-scale learning curves and runtime measurements are reported in Appendix~\ref{sec:appendix-runtime}.

\section{Limitations}
\label{sec:limitations}

CARAT is not a data-free defense. It assumes a labeled server-side reference set that covers the task label space, so it is most appropriate for cross-silo or institutionally managed FL settings where audit data are available. If the reference set is biased, too small, or missing important classes, hidden-task certificates may overestimate the utility of harmful updates outside the reference support.

The main empirical claim is also deliberately scoped. The matched comparison evaluates untargeted model poisoning on CIFAR-100/ResNet18 under $\alpha\in\{1,0.5\}$ and a $20\%$ attacker fraction. The experiments do not establish universal robustness against backdoor attacks, data poisoning, or all possible adaptive attacks against the hidden-task mechanism. More severe heterogeneity, larger client populations, and additional datasets should be treated as stress tests rather than as claims already settled by the main table.

Finally, CARAT adds server-side computation because every round evaluates candidate updates on multiple hidden tasks. The overhead is tunable through the number of tasks, probe samples, coordinate subsamples, and optimization steps, but deployments with strict server latency constraints may require caching, batching, or a smaller probe schedule.

\section{Conclusion}
\label{sec:conclusion}

We presented CARAT, a class-aware robust aggregation rule that validates client updates through hidden task certificates rather than relying only on geometric centrality or a single trusted reference direction. By scoring updates across multiple hidden, class-balanced semantic slices and combining those scores with rank-space anomaly suppression and capped-simplex weight optimization, CARAT is designed to preserve benign heterogeneity while reducing the influence of semantically harmful or structurally extreme clients.

The CIFAR-100/ResNet18 non-i.i.d.\ experiments support this design choice: CARAT achieves the best average rank against Mean, NormClipping, MultiKrum, FLTrust, FLDetector, and TriGuardFL across ALIE, FangAttack, MinMax, and MinSum for $\alpha\in\{1,0.5\}$. The evidence is strongest as a heterogeneous untargeted-poisoning result rather than as a universal poisoning claim. The method is most appropriate in FL deployments where the server can maintain a modest labeled audit set and where class-selective failures matter under heterogeneous client drift. The next empirical step is to broaden the stress-test suite across more severe heterogeneity, larger client populations, trusted-budget sweeps, and additional attack families.

\bibliographystyle{unsrtnat}
\bibliography{ref}

\newpage
\appendix

\section{Algorithmic Details}
\label{sec:appendix-algorithm}

\subsection{Round-Level Pseudocode}

Algorithm~\ref{alg:carat-round} summarizes one CARAT server round in standard pseudocode form.

\begin{algorithm}[t]
\caption{CARAT server aggregation at round $r$}
\label{alg:carat-round}
\begin{algorithmic}[1]
\REQUIRE Current global model $w_r$, client messages $\{u_i^r\}_{i=1}^n$, previous-round weights $\alpha^{\mathrm{prev}}$, probe pool $\mathcal{P}$, CARAT hyperparameters
\ENSURE Aggregated update $\Delta_{\mathrm{agg}}$, updated temporal prior $\alpha^{\mathrm{prev}}$
\IF{$\mathcal{P}$ is uninitialized}
    \STATE Build a balanced probe pool from the server-visible training dataset
\ENDIF
\IF{$r=1$}
    \STATE $\alpha^{\mathrm{prev}} \leftarrow \frac{1}{n}\mathbf{1}$
\ENDIF
\FOR{$i=1$ to $n$}
    \STATE Convert $u_i^r$ into a vector-form delta $\Delta_i^r$
\ENDFOR
\STATE Compute the clipping threshold $\tau_r$ from $\{\|\Delta_i^r\|_2\}_{i=1}^n$
\FOR{$i=1$ to $n$}
    \STATE $\bar{\Delta}_i^r \leftarrow \min\!\left(1,\frac{\tau_r}{\|\Delta_i^r\|_2+\varepsilon}\right)\Delta_i^r$
\ENDFOR
\STATE Choose the evaluation radius $q_r$ from a quantile of positive clipped norms
\FOR{$i=1$ to $n$}
    \STATE $\tilde{\Delta}_i^r \leftarrow q_r \bar{\Delta}_i^r / (\|\bar{\Delta}_i^r\|_2+\varepsilon)$
\ENDFOR
\STATE Sample $T$ class-balanced hidden tasks from $\mathcal{P}$ with replacement/subset fallback if strict balanced sampling fails
\FOR{$t=1$ to $T$}
    \STATE Evaluate the baseline loss $\ell_t(w_r)$
\ENDFOR
\FOR{$i=1$ to $n$}
    \FOR{$t=1$ to $T$}
        \STATE $c_{i,t} \leftarrow \ell_t(w_r) - \ell_t(w_r + \tilde{\Delta}_i^r)$
    \ENDFOR
\ENDFOR
\STATE Robustly standardize certificate columns to obtain $C^{\mathrm{std}}$
\STATE Compute rank penalties $r$ from repeated coordinate subsamples of $\{\bar{\Delta}_i^r\}_{i=1}^n$
\STATE $\alpha^{(0)} \leftarrow \alpha^{\mathrm{prev}}$
\FOR{$k=0$ to $K-1$}
    \STATE $z \leftarrow (C^{\mathrm{std}})^\top \alpha^{(k)}$
    \STATE $\pi \leftarrow \softmin$ task weights induced by $z$ and $\beta$
    \STATE $g \leftarrow C^{\mathrm{std}}\pi - \lambda_r r - 2\lambda_p(\alpha^{(k)}-\alpha^{\mathrm{prev}})$
    \STATE $\alpha^{(k+1)} \leftarrow \Pi_{\mathcal{A}(\hat{\rho})}\!\left(\alpha^{(k)}+\eta g\right)$
\ENDFOR
\STATE $\alpha^\star \leftarrow \alpha^{(K)}$ and $\alpha^{\mathrm{prev}} \leftarrow \alpha^\star$
\STATE $\Delta_{\mathrm{agg}} \leftarrow \sum_{i=1}^n \alpha_i^\star \bar{\Delta}_i^r$
\STATE \textbf{return} $\Delta_{\mathrm{agg}}, \alpha^{\mathrm{prev}}$
\end{algorithmic}
\end{algorithm}

\subsection{Implementation Notes}

\paragraph{Inputs.}
Current global model $w_r$, client messages $\{u_i^r\}_{i=1}^n$, previous-round weights $\alpha^{\mathrm{prev}}$, server-visible training dataset, and CARAT hyperparameters.

\paragraph{Persistent state.}
CARAT maintains two pieces of state across rounds: a balanced probe pool sampled at initialization from the server-visible training dataset, and the previous-round weight vector $\alpha^{\mathrm{prev}}$, which serves as the temporal prior for the current round.

\paragraph{Initialization.}
At the first round, the implementation initializes $\alpha^{\mathrm{prev}}$ to the uniform vector. This keeps the initial round unbiased across clients while still allowing later rounds to exploit temporal consistency once CARAT has observed several optimization trajectories.

\paragraph{Reference-pool construction and hidden-task sampling.}
Let $\mathcal{P}_c \subseteq \mathcal{P}$ denote the subset of probe examples with class label $c$. A hidden task is constructed by sampling a small number of examples from each available class pool and concatenating them into one balanced micro-task. If the desired number of samples per class is not available without replacement, CARAT falls back to replacement and, if needed, to a random subset of the probe pool rather than aborting the round. This fail-soft design preserves the intended semantic diversity of the task sampler while ensuring that the server always obtains a well-defined certificate matrix.

\paragraph{Robust score calibration.}
For notational economy, the main text reuses $C$ after standardization. In implementation terms, the raw certificate matrix is first transformed column-wise into
\[
C^{\mathrm{std}}_{i,t}
=
\frac{c_{i,t}-\operatorname{median}_j(c_{j,t})}
{1.4826 \cdot \operatorname{MAD}_j(c_{j,t})+\varepsilon}.
\]
This removes task-specific scale effects while preserving within-task ordering by Proposition~\ref{prop:task-standardization}. Rank penalties are calibrated similarly. For each coordinate subsample $S_s$, let
\[
\rho_i^{(s)}=
\frac{1}{|S_s|}\sum_{j \in S_s}
\left|
\frac{\operatorname{rank}_{i,j}}{n-1} - \frac{1}{2}
\right|.
\]
CARAT then computes a standardized rank score
\[
z_i^{(s)}=
\frac{\rho_i^{(s)}-\operatorname{median}_\ell(\rho_\ell^{(s)})}
{1.4826 \cdot \operatorname{MAD}_\ell(\rho_\ell^{(s)})+\varepsilon},
\]
and aggregates subsamples through
\[
r_i=\max\left(0,\operatorname{median}_s z_i^{(s)}\right).
\]
As a result, a client is penalized only when it is consistently extreme across repeated coordinate views, not because of a single noisy subsample.

\paragraph{Projected optimization details.}
Again writing $C$ for the standardized certificate matrix, CARAT initializes the optimizer with $\alpha^{(0)}=\alpha^{\mathrm{prev}}$ after the first round, and with the uniform vector at initialization. If
\[
z(\alpha)=C^\top \alpha,
\qquad
\pi_t(\alpha)=\frac{\exp(-\beta z_t(\alpha))}{\sum_{\ell=1}^T \exp(-\beta z_\ell(\alpha))},
\]
then the ascent direction used in projected updates is
\[
g(\alpha)=C\pi(\alpha)-\lambda_r r-2\lambda_p(\alpha-\alpha^{\mathrm{prev}}).
\]
With optimizer step size $\eta$, one iteration takes the form
\[
\alpha^{(k+1)}=
\Pi_{\mathcal{A}(\hat{\rho})}
\left(\alpha^{(k)}+\eta g(\alpha^{(k)})\right).
\]
The projection maintains feasibility throughout optimization, so every intermediate iterate already satisfies the nonnegativity, unit-sum, and per-client cap constraints described in the main text.

\paragraph{Round invariants and failure handling.}
Two implementation choices are important for robustness. First, the optimizer never leaves the capped simplex, so every intermediate and final weight vector remains feasible. Second, probe-task construction is fail-soft rather than fail-stop: if strict class-balanced sampling is impossible, the code falls back to replacement or to a random subset, ensuring that the round can still proceed with a well-defined certificate matrix.

\paragraph{Outputs.}
Aggregated server update, updated temporal prior $\alpha^{\mathrm{prev}}$, and diagnostic logs including learned weights, task scores, softmin focus, rank penalties, evaluation radius, clipping threshold, and the identities of the hidden probe tasks used in the current round.

\paragraph{Practical diagnostics.}
The most useful round-level diagnostics for debugging and empirical interpretation are the learned weights, the weakest-task values emphasized by the softmin, the clip threshold $\tau_r$, the evaluation radius $q_r$, and the final rank penalties $r_i$. In successful runs, these quantities help distinguish three qualitatively different cases: benign but diverse clients that retain moderate weight, clearly harmful clients that lose certificate support across multiple tasks, and structurally extreme clients whose influence is reduced primarily by the rank penalty.

\section{Theoretical Analysis}
\label{sec:appendix-theory}

This appendix records the main structural properties used to interpret CARAT. These results are not presented as a complete robustness guarantee against arbitrary adaptive attackers. Instead, they clarify why the method decomposes into a normalized evaluation geometry, a task-aware utility objective, and a constrained robust aggregation step.

\begin{proposition}[Feasibility and support of the capped simplex]
\label{prop:capped-simplex}
Let
\[
\mathcal{A}(\hat{\rho})=
\left\{
\alpha \in \R^n:
\alpha_i \ge 0,\;
\sum_{i=1}^n \alpha_i = 1,\;
\alpha_i \le \frac{1}{(1-\hat{\rho})n}
\right\},
\]
with $\hat{\rho}<1$. Then $\mathcal{A}(\hat{\rho})$ is non-empty, and every $\alpha \in \mathcal{A}(\hat{\rho})$ satisfies
\[
|\operatorname{supp}(\alpha)| \ge (1-\hat{\rho})n.
\]
\end{proposition}

\begin{proof}[Proof sketch]
Let $u = 1/((1-\hat{\rho})n)$. Since $\hat{\rho}<1$, we have $u \ge 1/n$, so the uniform weight vector is feasible and the set is non-empty. For the support bound, if only $k$ coordinates of $\alpha$ are positive, then
\[
1=\sum_{i=1}^n \alpha_i \le k u = \frac{k}{(1-\hat{\rho})n},
\]
which implies $k \ge (1-\hat{\rho})n$. Therefore CARAT can suppress suspicious clients, but cannot collapse the aggregate onto an arbitrarily small subset of participants.
\end{proof}

\begin{proposition}[Softmin bounds and gradient form]
\label{prop:softmin}
Let $s \in \R^T$ and define
\[
\softmin_{\beta}(s)
= -\frac{1}{\beta}\log\left(\frac{1}{T}\sum_{t=1}^T e^{-\beta s_t}\right).
\]
Then
\[
\min_t s_t \le \softmin_{\beta}(s) \le \min_t s_t + \frac{\log T}{\beta}.
\]
Moreover,
\[
\lim_{\beta \to \infty}\softmin_{\beta}(s)=\min_t s_t,
\qquad
\lim_{\beta \to 0}\softmin_{\beta}(s)=\frac{1}{T}\sum_{t=1}^T s_t.
\]
Moreover, if $z(\alpha)=C^\top\alpha$ and
\[
\pi_t(\alpha)=\frac{\exp(-\beta z_t(\alpha))}{\sum_{\ell=1}^T \exp(-\beta z_\ell(\alpha))},
\]
then
\[
\nabla_\alpha \softmin_{\beta}(C^\top\alpha)=C\pi(\alpha).
\]
\end{proposition}

\begin{proof}[Proof sketch]
Let $m=\min_t s_t$. Since
\[
e^{-\beta m}/T \le \frac{1}{T}\sum_{t=1}^T e^{-\beta s_t} \le e^{-\beta m},
\]
applying $-(1/\beta)\log(\cdot)$ yields
\[
m \le \softmin_{\beta}(s) \le m+\frac{\log T}{\beta}.
\]
The limit $\beta \to \infty$ follows immediately from the vanishing gap bound. For $\beta \to 0$, a first-order expansion of the exponential average gives
\[
\frac{1}{T}\sum_{t=1}^T e^{-\beta s_t}
=
1-\beta \frac{1}{T}\sum_{t=1}^T s_t + o(\beta),
\]
so the softmin converges to the arithmetic mean. Differentiating the softmin expression with respect to $z_t$ yields the Gibbs weights $\pi_t(\alpha)$, and the chain rule then gives $C\pi(\alpha)$. Hence the ascent direction is a convex combination of certificate columns, with more weight placed on tasks whose current aggregate utility is smaller.
\end{proof}

\begin{proposition}[Directional invariance of certificate scoring]
\label{prop:directional-invariance}
Suppose two nonzero clipped updates satisfy $\bar{\Delta}_i = a \bar{\Delta}_j$ for some scalar $a>0$. Then their normalized versions in Eq.~\eqref{eq:normalize} coincide, i.e.,
\[
\tilde{\Delta}_i=\tilde{\Delta}_j.
\]
Consequently, their hidden-task certificates are identical:
\[
c_{i,t}=c_{j,t}\qquad \text{for all } t \in \{1,\dots,T\}.
\]
\end{proposition}

\begin{proof}[Proof sketch]
Eq.~\eqref{eq:normalize} rescales every nonzero clipped update to the common radius $q_r$. Positive collinearity is therefore removed by normalization:
\[
q_r\frac{\bar{\Delta}_i}{\|\bar{\Delta}_i\|_2}
=
q_r\frac{a\bar{\Delta}_j}{\|a\bar{\Delta}_j\|_2}
=
q_r\frac{\bar{\Delta}_j}{\|\bar{\Delta}_j\|_2}.
\]
Substituting the resulting equality into Eq.~\eqref{eq:cert} gives identical task scores. Thus, after clipping, the certificate stage depends on direction rather than residual norm magnitude.
\end{proof}

\begin{proposition}[Task-wise standardization preserves within-task ordering]
\label{prop:task-standardization}
Fix a task $t$ and define a standardized certificate score
\[
\hat{c}_{i,t}=\frac{c_{i,t}-m_t}{s_t+\varepsilon},
\]
where $m_t \in \R$ and $s_t>0$ depend only on task $t$ and not on client $i$. Then for any clients $i,j$,
\[
c_{i,t} \ge c_{j,t}
\quad \Longleftrightarrow \quad
\hat{c}_{i,t} \ge \hat{c}_{j,t}.
\]
Moreover,
\[
\hat{c}_{i,t}-\hat{c}_{j,t}
=
\frac{c_{i,t}-c_{j,t}}{s_t+\varepsilon}.
\]
\end{proposition}

\begin{proof}[Proof sketch]
For a fixed task $t$, the map $x \mapsto (x-m_t)/(s_t+\varepsilon)$ is affine with strictly positive slope because $s_t+\varepsilon>0$. Hence it preserves ordering. The difference identity follows by direct subtraction. Therefore column-wise robust standardization calibrates cross-task scale without changing the within-task ranking induced by the raw certificate scores.
\end{proof}

\begin{proposition}[Temporal prior and projected-iterate invariants]
\label{prop:temporal-prior}
Define
\[
f(\alpha)=\softmin_{\beta}(C^\top\alpha)-\lambda_r r^\top\alpha-\lambda_p\|\alpha-\alpha^{\mathrm{prev}}\|_2^2.
\]
If $\alpha,\alpha' \in \mathcal{A}(\hat{\rho})$ satisfy $C^\top\alpha=C^\top\alpha'$ and $r^\top\alpha=r^\top\alpha'$, then
\[
f(\alpha)-f(\alpha')
=
-\lambda_p\!\left(\|\alpha-\alpha^{\mathrm{prev}}\|_2^2-\|\alpha'-\alpha^{\mathrm{prev}}\|_2^2\right).
\]
In addition, under projected gradient ascent
\[
\alpha^{(k+1)}=\Pi_{\mathcal{A}(\hat{\rho})}\!\left(\alpha^{(k)}+\eta \nabla f(\alpha^{(k)})\right),
\]
every iterate remains in $\mathcal{A}(\hat{\rho})$.
\end{proposition}

\begin{proof}[Proof sketch]
The first statement follows by direct substitution: if the certificate and rank terms match, the only remaining difference in objective value is the quadratic prior term. CARAT therefore prefers, among utility-equivalent feasible weights, the solution closer to $\alpha^{\mathrm{prev}}$. The second statement is immediate from the projection operator. Hence the optimizer preserves three round invariants throughout: $\alpha_i \ge 0$, $\sum_i \alpha_i =1$, and $\alpha_i \le 1/((1-\hat{\rho})n)$.
\end{proof}

\begin{proposition}[Pairwise weight-transfer condition]
\label{prop:pairwise-transfer}
Let
\[
f(\alpha)=\softmin_{\beta}(C^\top\alpha)-\lambda_r r^\top\alpha-\lambda_p\|\alpha-\alpha^{\mathrm{prev}}\|_2^2
\]
be the CARAT objective, and let $e_a,e_b$ be coordinate vectors for two clients $a$ and $b$. Suppose $\alpha+\epsilon(e_b-e_a)$ remains feasible for sufficiently small $\epsilon>0$. Define
\[
\pi_t(\alpha)=\frac{\exp(-\beta (C^\top\alpha)_t)}
{\sum_{\ell=1}^T \exp(-\beta (C^\top\alpha)_\ell)}.
\]
Then moving infinitesimal weight from client $a$ to client $b$ increases the objective to first order whenever
\[
(C_b-C_a)^\top \pi(\alpha)
-\lambda_r(r_b-r_a)
-2\lambda_p\!\left[
(\alpha_b-\alpha_b^{\mathrm{prev}})
-(\alpha_a-\alpha_a^{\mathrm{prev}})
\right] > 0,
\]
where $C_i$ denotes row $i$ of the standardized certificate matrix.
\end{proposition}

\begin{proof}[Proof sketch]
Let $d=e_b-e_a$. A first-order expansion gives
\[
f(\alpha+\epsilon d)-f(\alpha)
=\epsilon d^\top\nabla f(\alpha)+o(\epsilon).
\]
Using Proposition~\ref{prop:softmin}, the gradient is
\[
\nabla f(\alpha)=C\pi(\alpha)-\lambda_r r-2\lambda_p(\alpha-\alpha^{\mathrm{prev}}).
\]
Substituting $d=e_b-e_a$ yields exactly the displayed expression. Hence a client gains weight when its hidden-task utility advantage, measured through the current softmin focus $\pi(\alpha)$, outweighs its rank penalty and temporal-prior cost.
\end{proof}

\begin{proposition}[Norm bound for the final aggregate]
\label{prop:aggregate-bound}
Suppose each clipped update satisfies $\|\bar{\Delta}_i\|_2 \le \tau_r$ and $\alpha \in \mathcal{A}(\hat{\rho})$. Then the final aggregate
\[
\Delta_{\mathrm{agg}}=\sum_{i=1}^n \alpha_i \bar{\Delta}_i
\]
satisfies
\[
\|\Delta_{\mathrm{agg}}\|_2 \le \tau_r.
\]
\end{proposition}

\begin{proof}[Proof sketch]
Because $\alpha_i \ge 0$ and $\sum_i \alpha_i=1$, the aggregate is a convex combination of clipped updates. By the triangle inequality,
\[
\|\Delta_{\mathrm{agg}}\|_2
\le
\sum_{i=1}^n \alpha_i \|\bar{\Delta}_i\|_2
\le
\sum_{i=1}^n \alpha_i \tau_r
=
\tau_r.
\]
Thus the final server step inherits the clipping radius as a hard norm upper bound.
\end{proof}

\begin{proposition}[Influence bound from malicious weight mass]
\label{prop:malicious-mass-bound}
Let $\mathcal{M}$ be the malicious clients selected in a round and let
\[
q_{\mathcal{M}}=\sum_{i\in\mathcal{M}}\alpha_i
\]
be their total CARAT weight. Suppose every clipped update satisfies $\|\bar{\Delta}_i\|_2\le\tau_r$. Then the direct malicious contribution obeys
\[
\left\|\sum_{i\in\mathcal{M}}\alpha_i\bar{\Delta}_i\right\|_2
\le q_{\mathcal{M}}\tau_r.
\]
If $q_{\mathcal{M}}<1$ and
\[
\Delta_{\mathcal{B}}=
\sum_{i\notin\mathcal{M}}\frac{\alpha_i}{1-q_{\mathcal{M}}}\bar{\Delta}_i
\]
denotes the benign-only aggregate obtained by renormalizing the benign weights, then
\[
\|\Delta_{\mathrm{agg}}-\Delta_{\mathcal{B}}\|_2
\le 2q_{\mathcal{M}}\tau_r.
\]
\end{proposition}

\begin{proof}[Proof sketch]
The first inequality follows from the triangle inequality:
\[
\left\|\sum_{i\in\mathcal{M}}\alpha_i\bar{\Delta}_i\right\|_2
\le
\sum_{i\in\mathcal{M}}\alpha_i\|\bar{\Delta}_i\|_2
\le q_{\mathcal{M}}\tau_r.
\]
For the second inequality, write
\[
\Delta_{\mathrm{agg}}=
(1-q_{\mathcal{M}})\Delta_{\mathcal{B}}
+\sum_{i\in\mathcal{M}}\alpha_i\bar{\Delta}_i.
\]
Therefore
\[
\|\Delta_{\mathrm{agg}}-\Delta_{\mathcal{B}}\|_2
\le
q_{\mathcal{M}}\|\Delta_{\mathcal{B}}\|_2
+\left\|\sum_{i\in\mathcal{M}}\alpha_i\bar{\Delta}_i\right\|_2.
\]
Since $\Delta_{\mathcal{B}}$ is a convex combination of clipped benign updates, $\|\Delta_{\mathcal{B}}\|_2\le\tau_r$ by the same argument as Proposition~\ref{prop:aggregate-bound}. Combining this with the first inequality gives the bound.
\end{proof}

\begin{proposition}[Per-round complexity]
\label{prop:complexity}
Let $n$ be the number of clients, $T$ the number of hidden tasks, $m$ the number of sampled coordinates for rank scoring, $S$ the number of rank subsamples, and $K$ the number of projected-gradient steps. Ignoring constant factors from batch size and model width, the extra defense-side work of CARAT scales as
\[
\text{probe-model forward passes} + O(Snm + KnT).
\]
\end{proposition}

\begin{proof}[Proof sketch]
CARAT performs one baseline probe evaluation plus $n$ candidate probe evaluations over the $T$ hidden tasks, then computes $S$ rank summaries over $m$ coordinates, and finally executes $K$ projected optimization steps over $n$ weights and $T$ task scores. In the prototype implementation, the probe-model forward passes dominate wall-clock time, while the rank and optimization stages are lower-order terms controlled by $m$, $S$, and $K$.
\end{proof}

\paragraph{Remark on assumptions.}
The object $\mathcal{D}^{\mathrm{ref}}$ in the main text denotes the labeled server reference set, while the probe pool $\mathcal{P}$ used during each round is a subsampled working set derived from it. In practice, the server first constructs $\mathcal{P}$ from the full server-visible reference data and then samples hidden tasks from $\mathcal{P}$ at each round. This distinction should remain explicit because it is part of the method's server-side assumption and materially strengthens the setting relative to a fully data-free defense.

\section{Additional Experimental Details}
\label{sec:appendix-experiments}

This appendix records the benchmark design, sensitivity analyses, and reporting scope that support the main-text experimental design.

\subsection{Experimental Goals}

The empirical study is designed to test properties specific to CARAT rather than to benchmark it as a generic outlier filter. In particular, we ask whether hidden-task validation improves robustness when benign clients are heterogeneous, whether that advantage persists on class-rich benchmarks where class-selective failure matters, and how much robustness is purchased per unit of trusted-data budget and runtime overhead.

\subsection{Benchmarks}

\paragraph{Primary class-rich benchmark.}
The main benchmark is CIFAR-100 with ResNet18. This setting best matches CARAT's motivation because the label space is rich enough for different hidden tasks to emphasize different semantic slices, and because average clean accuracy alone can hide substantial per-class degradation.

\paragraph{Scalability benchmark.}
TinyImageNet with ResNet18 is used to test whether the same aggregation rule remains effective as the number of classes and the visual vocabulary increase. This benchmark is particularly useful for checking whether the hidden-task construction and reference-budget design remain meaningful beyond CIFAR-100.

\paragraph{Application-oriented benchmark.}
We include CHMNIST with ResNet18 as an application-style benchmark in the appendix. Its role is not to maximize scale, but to show that class-selective failures have a practical interpretation in a domain where server-side auditing with a small labeled reference set is plausible.

\paragraph{Sanity-check benchmark.}
CIFAR-10 is retained only as a sanity-check benchmark. It remains useful for verifying that CARAT does not fail on standard low-cardinality image classification, but it is not treated as a primary source of evidence because its ten classes are too few to fully exercise the method's class-aware design.

\subsection{Threat Models}

\paragraph{Untargeted poisoning.}
The main untargeted suite contains ALIE, FangAttack, MinMax, and MinSum. Together they represent adaptive model-poisoning attacks that aim to remain stealthy under server-side defenses, plus a geometry-aware stressor that is especially informative for Krum-style and history-based defenses.

\subsection{Baselines}

\paragraph{Untargeted defenses.}
For untargeted poisoning, the primary comparison uses Mean, NormClipping, MultiKrum, FLTrust, FLDetector, TriGuardFL, and CARAT. This set spans plain averaging, norm-based clipping, geometry-based filtering, trusted-data anchoring, historical-trajectory detection, and class-aware hidden-task validation. TrimmedMean remains useful for smaller verification runs, but it is not part of the CIFAR-100/ResNet18 non-i.i.d.\ comparison block.

\subsection{Metrics and Fairness}

No-attack clean utility is reported separately from attacked runs. The clean metrics are test accuracy and test loss. On class-rich datasets we additionally report macro-average accuracy and worst-class accuracy, because the motivation for CARAT is precisely that averaging can hide semantically concentrated failures. Runtime per round and total defense overhead are reported for all defenses that add nontrivial server-side computation.

Trusted-data fairness is controlled explicitly. CARAT and FLTrust are compared under the same labeled server-reference budget, rather than allowing one method to benefit from more public data. In addition to fixed-budget comparisons, we sweep the reference budget in the appendix to show whether CARAT's advantage comes from the hidden-task validation principle rather than from budget alone.

\subsection{Shared Training Protocol}

Unless stated otherwise, the primary comparison uses $20$ clients with full participation, $200$ communication rounds, $4$ local epochs, and learning rate $0.05$. The main heterogeneity settings are Dirichlet non-i.i.d.\ partitions with $\alpha \in \{1,0.5\}$; the more severe $\alpha=0.1$ setting is reserved for stress testing rather than used as a main-text claim. The main attacker ratio is $20\%$, while $40\%$ is reserved for appendix stress tests. Baseline rows use five random seeds. CARAT seed counts are reported in the relevant table captions. Scaling studies in the appendix may additionally vary the number of clients or the attacker ratio, but these are treated as stress tests rather than as the core evaluation axis.

\subsection{Final Loss Results}

Table~\ref{tab:cifar100-main-loss} reports the final test loss values corresponding to the main-text accuracy comparison. It is included in the appendix because it supports the same conclusion as Table~\ref{tab:cifar100-main-acc}: CARAT obtains the best average rank under both non-i.i.d.\ regimes.

\begin{table*}[!htbp]
\centering
\caption{Final test loss under four model-poisoning attacks on CIFAR-100/ResNet18 with $20\%$ malicious clients and non-i.i.d.\ client partitions. Lower is better. Baseline cells use five seeds. CARAT cells use $n=2$ for $\alpha=1$ ALIE, $n=1$ for $\alpha=0.5$ MinSum, and $n=3$ otherwise.}
\label{tab:cifar100-main-loss}
\renewcommand{\arraystretch}{1.05}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{llccccc}
\toprule
\textbf{Dirichlet $\alpha$} & \textbf{Defense} & \textbf{ALIE} & \textbf{FangAttack} & \textbf{MinMax} & \textbf{MinSum} & \textbf{Avg. rank} \\
\midrule
\textbf{$1.0$} & Mean & $0.0292 \pm 0.0004$ & $0.0294 \pm 0.0004$ & $0.0283 \pm 0.0004$ & $0.0283 \pm 0.0006$ & $3.2$ \\
\textbf{$1.0$} & NormClipping & $0.0476 \pm 0.0005$ & $0.0501 \pm 0.0004$ & $0.0484 \pm 0.0004$ & $0.0485 \pm 0.0003$ & $7.0$ \\
\textbf{$1.0$} & MultiKrum & $0.0361 \pm 0.0006$ & $0.0377 \pm 0.0005$ & $0.0295 \pm 0.0005$ & $0.0301 \pm 0.0009$ & $4.8$ \\
\textbf{$1.0$} & FLTrust & $0.0409 \pm 0.0004$ & $0.0407 \pm 0.0006$ & $0.0406 \pm 0.0007$ & $0.0406 \pm 0.0005$ & $5.8$ \\
\textbf{$1.0$} & FLDetector & $0.0305 \pm 0.0004$ & $0.0450 \pm 0.0035$ & $0.0283 \pm 0.0006$ & $0.0284 \pm 0.0003$ & $4.0$ \\
\textbf{$1.0$} & TriGuardFL & $0.0291 \pm 0.0004$ & $0.0280 \pm 0.0005$ & $0.0283 \pm 0.0004$ & $0.0283 \pm 0.0004$ & $2.2$ \\
\textbf{$1.0$} & \textbf{CARAT} & \textbf{0.0267$\pm$0.0009} & \textbf{0.0260$\pm$0.0005} & \textbf{0.0260$\pm$0.0007} & \textbf{0.0260$\pm$0.0010} & \textbf{1.0} \\
\midrule
\textbf{$0.5$} & Mean & $0.0303 \pm 0.0005$ & $0.0302 \pm 0.0006$ & $0.0293 \pm 0.0001$ & $0.0293 \pm 0.0004$ & $3.2$ \\
\textbf{$0.5$} & NormClipping & $0.0486 \pm 0.0005$ & $0.0517 \pm 0.0011$ & $0.0497 \pm 0.0005$ & $0.0498 \pm 0.0004$ & $7.0$ \\
\textbf{$0.5$} & MultiKrum & $0.0382 \pm 0.0008$ & $0.0407 \pm 0.0010$ & $0.0306 \pm 0.0006$ & $0.0308 \pm 0.0006$ & $4.8$ \\
\textbf{$0.5$} & FLTrust & $0.0420 \pm 0.0005$ & $0.0417 \pm 0.0005$ & $0.0417 \pm 0.0006$ & $0.0417 \pm 0.0004$ & $5.8$ \\
\textbf{$0.5$} & FLDetector & $0.0319 \pm 0.0004$ & $0.0458 \pm 0.0011$ & $0.0294 \pm 0.0004$ & $0.0292 \pm 0.0005$ & $4.0$ \\
\textbf{$0.5$} & TriGuardFL & $0.0301 \pm 0.0004$ & $0.0290 \pm 0.0003$ & $0.0292 \pm 0.0004$ & $0.0292 \pm 0.0007$ & $2.2$ \\
\textbf{$0.5$} & \textbf{CARAT} & \textbf{0.0271$\pm$0.0006} & \textbf{0.0266$\pm$0.0010} & \textbf{0.0259$\pm$0.0003} & \textbf{0.0253} & \textbf{1.0} \\
\bottomrule
\end{tabular}
}
\end{table*}

\subsection{Verification-Scale Runtime Results}
\label{sec:appendix-runtime}

Figure~\ref{fig:verification-curves} reports a smaller CIFAR-100/i.i.d.\ ALIE verification run that records both round-wise accuracy and server-side runtime. In this setting, CARAT remains stable across seeds under attack but pays a clear runtime premium because each round evaluates candidate updates on repeated hidden tasks. The result is not used as the main robustness claim; it is included to make the overhead tradeoff visible and to motivate the budget controls in the ablation plan.

\begin{figure*}[!htbp]
\centering
\safeincludegraphics[width=0.98\textwidth]{figures/carat_verification.pdf}
\caption{Verification-scale local results on CIFAR-100 under the ALIE model-poisoning attack. Left: round-wise test accuracy, where solid curves show the mean over three seeds and shaded bands denote one standard deviation; the dashed clean Mean curve is a single-seed no-attack reference. Right: average per-round wall-clock time with seed-level error bars, showing the defense overhead of repeated hidden-task evaluation.}
\label{fig:verification-curves}
\end{figure*}

\subsection{Supplementary Experiment Scope}

The main-text comparison is intentionally narrower than the full experimental implementation. In particular, the implementation supports a broader set of model-poisoning attacks and defense baselines than the primary CARAT comparison uses. Table~\ref{tab:expansion-matrix} separates the core suite used for the main claim from additional experiment axes that are most useful for stress testing, sensitivity analysis, and future extensions. Backdoor and data-poisoning implementations remain outside the claim set of this paper.

\begin{table*}[!htbp]
\centering
\caption{Supplementary model-poisoning experiment scope. The core suite matches the main-text claim, while the extension suite records additional comparisons supported by the implementation.}
\label{tab:expansion-matrix}
\renewcommand{\arraystretch}{1.1}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.18\textwidth}p{0.25\textwidth}p{0.31\textwidth}p{0.22\textwidth}}
\toprule
\textbf{Axis} & \textbf{Core suite} & \textbf{Extension suite} & \textbf{Suggested visualization} \\
\midrule
Untargeted attacks & ALIE, FangAttack, MinMax, MinSum & Gaussian, IPM, Mimic, SignFlipping, ThreeDFed & Final robustness tables; round-wise accuracy curves \\
Untargeted defenses & Mean, NormClipping, MultiKrum, FLTrust, FLDetector, TriGuardFL, CARAT & Median, TrimmedMean, Krum, Bulyan, RFA, DnC, Auror, LASA, FoolsGold, SignGuard, CenteredClipping, Bucketing, SimpleClustering & Grouped comparison tables; per-attack win/loss summaries \\
Data regimes & Dirichlet non-i.i.d.\ $\alpha \in \{1,0.5\}$ & $\alpha=0.1$, i.i.d.\ verification runs, high-attacker stress tests ($40\%$), client-count scaling, CIFAR-10 sanity checks, TinyImageNet, CHMNIST & Robustness-vs-heterogeneity curves; benchmark summary tables \\
Efficiency and fairness & Matched CARAT / FLTrust reference budget; runtime per round & Reference-budget sweeps; runtime breakdowns; trusted-data sensitivity & Overhead bars; budget-response curves; clean-robustness-overhead tradeoff plots \\
Out-of-scope implemented attacks and defenses & -- & ModelReplacement, DBA, Neurotoxin, BadNets, EdgeCase, AlterMin, LabelFlipping, FLAME, DeepSight, CRFL & Keep outside the claim set unless the paper scope is widened \\
\bottomrule
\end{tabular}
}
\end{table*}

\subsection{CARAT Hyperparameters, Stress Tests, and Ablation Plan}

The implementation defaults are $8$ hidden tasks, $4$ probe samples per class, probe batch size $128$, probe-radius quantile $0.5$, MAD clipping with $k=2.5$, default attacker prior $\hat{\rho}=0.2$, certificate temperature $\beta=12.0$, rank weight $0.20$, prior weight $0.05$, $4096$ sampled coordinates, $5$ rank subsamples, $60$ projected optimization steps, and optimizer learning rate $0.5$.

Two design principles matter when transferring these defaults across datasets. First, the reference budget should scale with the number of classes rather than be fixed as a single dataset-independent total pool size. Second, CARAT should be ablated along the axes that correspond directly to its mechanism: hidden-task count, reference budget per class, rank-penalty weight, temporal-prior weight, and clipping controls. The appendix therefore includes ablations that isolate whether the observed robustness comes from class-balanced hidden tasks, from the structural rank penalty, or simply from access to more trusted data. It also records high-attacker stress tests, scaling analyses, and runtime breakdowns so that robustness, efficiency, and trusted-data dependence are evaluated together.

\section{Reproducibility, Ethics, and Availability}
\label{sec:appendix-reproducibility}

\paragraph{Reproducibility.}
All main reported results use public benchmark datasets and fixed training protocols described in Section~\ref{sec:experiments}. The aggregation method is specified in Algorithm~\ref{alg:carat-round}, with implementation defaults listed above. For anonymous review, logs, plotting scripts, and configuration files should be included in the supplementary material or an anonymized archive so that each table cell can be traced to its seed-level runs.

\paragraph{Compute resources.}
Experiments were launched on a Compute Canada SLURM cluster through the repository launch scripts. The primary CIFAR-100/CARAT 200-round runs requested one NVIDIA H100 80GB HBM3 MIG GPU slice with the SLURM resource string \texttt{nvidia\_h100\_80gb\_hbm3\_2g.20gb:1}, $8$ CPU cores, $64$GB host memory, and a $24$ hour wall-time limit per array task; the launcher used $4$ data-loading workers and capped concurrent array tasks at $3$ for this main CARAT specification. Supplementary CARAT specifications used the same single-GPU resource profile with wall-time limits between $4$ hours and $2$ days and array concurrency between $1$ and $4$, depending on the benchmark and sweep size. Each run writes a metadata file recording the configuration file, command line, seed, Python environment, CUDA requirement, and git commit.

\paragraph{Ethics.}
This work studies defenses against model-poisoning attacks in federated learning. The attack implementations are used to evaluate robustness and do not require access to private user data. The main practical risk is dual use: stronger attack simulations may help adversaries tune poisoning strategies. We mitigate this by framing attacks as benchmark stressors and by releasing defensive evaluation code together with the corresponding protocols.

\paragraph{Data availability.}
CIFAR-10, CIFAR-100, TinyImageNet, and CHMNIST are public datasets. CARAT uses a small labeled server-side reference split during evaluation; when reproducing the results, this split should be generated deterministically from the public training data under the same seed and excluded from client local training data.

\newpage
\input{checklist}

\end{document}
